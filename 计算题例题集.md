# 操作系统 计算题例题集

> 课程：Operating Systems (H), Spring 2026, Prof. Yinqian Zhang
> 说明：从 L02-L13 课件中识别所有可出计算题的知识点，每道题附详细解答

---

## 题目 1：SJF 调度 — 非抢占式

**知识点**：Non-preemptive SJF 的 Waiting Time 和 Turnaround Time 计算
**来源**：L08 CPU Scheduling

### 题目

假设有以下进程集合：

| Process | Arrival Time | CPU Burst Time |
|---------|:-----------:|:--------------:|
| P1      | 0           | 7              |
| P2      | 2           | 4              |
| P3      | 4           | 1              |
| P4      | 5           | 4              |

使用**非抢占式 SJF** 调度（平局时使用 FIFO）：
1. 画出 Gantt Chart
2. 计算每个进程的 Waiting Time 和 Turnaround Time
3. 计算 Average Waiting Time 和 Average Turnaround Time

### 解答

**Step 1 — 确定执行顺序**：
- t=0：只有 P1 到达 → P1 运行 7 单位（t=0 到 t=7）
- t=7：P2(4), P3(1), P4(4) 都已到达。选最短的 P3(1)。
- t=7-8：P3 运行 → P3 完成。
- t=8：P2(4) 和 P4(4) 需相同时间 → FIFO 规则，P2 先到先运行。
- t=8-12：P2 运行 → P2 完成。
- t=12-16：P4 运行 → P4 完成。

**Gantt Chart**：
```
| P1            | P3 | P2        | P4        |
0                7    8           12          16
```

**Step 2 — 计算**：

| Process | Arrival | Burst | Completion | Turnaround (CT-AT) | Waiting (TT-BT) |
|---------|:-------:|:-----:|:----------:|:------------------:|:---------------:|
| P1 | 0 | 7 | 7 | 7 - 0 = **7** | 7 - 7 = **0** |
| P2 | 2 | 4 | 12 | 12 - 2 = **10** | 10 - 4 = **6** |
| P3 | 4 | 1 | 8 | 8 - 4 = **4** | 4 - 1 = **3** |
| P4 | 5 | 4 | 16 | 16 - 5 = **11** | 11 - 4 = **7** |

- **Average Waiting Time** = (0 + 6 + 3 + 7) / 4 = **4**
- **Average Turnaround Time** = (7 + 10 + 4 + 11) / 4 = **8**
- **Context Switches** = 3

---

## 题目 2：SJF 调度 — 抢占式（SRTF）

**知识点**：Preemptive SJF (SRTF) 的 Waiting Time 和 Turnaround Time 计算
**来源**：L08 CPU Scheduling

### 题目

使用与题目 1 相同的数据，但使用**抢占式 SJF (SRTF)** 调度。每当新进程到达时，如果其剩余 CPU 时间比当前运行进程的剩余时间更短，则抢占 CPU。

### 解答

**Step 1 — 执行过程**：

| Time | 事件 |
|:----:|------|
| t=0 | P1 开始运行，剩余 7 |
| t=2 | P2 到达，P2 剩余 4 < P1 剩余 5 → **P2 抢占 P1** |
| t=4 | P3 到达，P3 剩余 1 < P2 剩余 2 → **P3 抢占 P2** |
| t=5 | P3 完成（CPU burst=1）。此时 P4 也到达。比较：P1 剩 5，P2 剩 2，P4 剩 4 → **P2 最短，运行** |
| t=5-7 | P2 运行剩余 2 单位 → P2 完成 |
| t=7-11 | P4 运行 4 单位 → P4 完成 |
| t=11-16 | P1 运行剩余 5 单位 → P1 完成 |

**Gantt Chart**：
```
| P1  | P2 | P3 | P2  | P4        | P1          |
0     2    4    5    7             11            16
```

**Step 2 — 计算**：

| Process | Arrival | Burst | Completion | Turnaround | Waiting |
|---------|:-------:|:-----:|:----------:|:----------:|:-------:|
| P1 | 0 | 7 | 16 | 16 - 0 = **16** | 16 - 7 = **9** |
| P2 | 2 | 4 | 7 | 7 - 2 = **5** | 5 - 4 = **1** |
| P3 | 4 | 1 | 5 | 5 - 4 = **1** | 1 - 1 = **0** |
| P4 | 5 | 4 | 11 | 11 - 5 = **6** | 6 - 4 = **2** |

- **Average Waiting Time** = (9 + 1 + 0 + 2) / 4 = **3**
- **Average Turnaround Time** = (16 + 5 + 1 + 6) / 4 = **7**
- **Context Switches** = 5

**对比非抢占式 SJF**：抢占式以更多上下文切换(5 vs 3)换取了更优的等待时间(3 vs 4)和周转时间(7 vs 8)。

---

## 题目 3：Round Robin 调度

**知识点**：RR (Quantum=3) 的 Waiting Time 和 Turnaround Time 计算
**来源**：L08 CPU Scheduling

### 题目

使用相同数据，使用 **Round Robin (Quantum = 3)** 调度。新进程到达时加入就绪队列末尾，不触发抢占。

### 解答

**Step 1 — 执行过程**：

| Time | 事件 | 就绪队列 |
|:----:|------|----------|
| t=0 | P1 运行(Q=3) | [] |
| t=2 | P2 到达 → 加入队尾 | [P2] |
| t=3 | P1 用完 Q → P1→队尾(Q=3)；P2 运行 | [P1] |
| t=4 | P3 到达 → 队尾 | [P1, P3] |
| t=5 | P4 到达 → 队尾 | [P1, P3, P4] |
| t=6 | P2 用完 Q → P2→队尾(Q=3)；P1 运行(剩4,Q=3) | [P3, P4, P2] |
| t=9 | P1 用完 Q(剩1) → P1→队尾(Q=3)；P3 运行(Q=3) | [P4, P2, P1] |
| t=10 | P3 完成(B=1) → 出队。P4 运行(Q=3) | [P2, P1] |
| t=13 | P4 用完 Q(剩1) → P4→队尾(Q=3)；P2 运行(剩1,Q=3) | [P1, P4] |
| t=14 | P2 完成(B=1) → 出队。P1 运行(剩1,Q=3) | [P4] |
| t=15 | P1 完成(B=1) → 出队。P4 运行(剩1,Q=3) | [] |
| t=16 | P4 完成 | [] |

**Gantt Chart**：
```
| P1    | P2    | P1    | P3 | P4    | P2 | P1 | P4 |
0       3       6       9   10       13   14   15   16
```

**Step 2 — 计算**：

| Process | Arrival | Burst | Completion | Turnaround | Waiting |
|---------|:-------:|:-----:|:----------:|:----------:|:-------:|
| P1 | 0 | 7 | 15 | **15** | **8** |
| P2 | 2 | 4 | 14 | **12** | **8** |
| P3 | 4 | 1 | 10 | **6** | **5** |
| P4 | 5 | 4 | 16 | **11** | **7** |

- **Average Waiting Time** = (8 + 8 + 5 + 7) / 4 = **7**
- **Average Turnaround Time** = (15 + 12 + 6 + 11) / 4 = **11**
- **Context Switches** = 7

**三种算法汇总对比**：

| 指标 | Non-preemptive SJF | Preemptive SJF | RR (Q=3) |
|------|:---:|:---:|:---:|
| Avg Waiting Time | 4 | 3 ✓ | 7 ✗ |
| Avg Turnaround | 8 | 7 ✓ | 11 ✗ |
| Context Switches | 3 | 5 | 7 ✗ |
| **响应性** | 差 | 中 | **优** ✓ |

---

## 题目 4：页面置换算法（Optimal / FIFO / LRU）

**知识点**：三种置换算法的 Page Fault 计数
**来源**：L06 Demand Paging

### 题目

假设系统有 **3 个页帧**，4 个虚拟页（A, B, C, D），参考串为：
```
A B C A B D A D B C B
```
分别计算 Optimal (MIN)、FIFO、LRU 算法的 Page Fault 次数。

### 解答 — Optimal (MIN)

规则：替换**未来最长时间内不会被使用**的页。

| Ref | Page Frames | Fault? | 说明 |
|:---:|------------|:------:|------|
| A | A - - | ✓ | |
| B | A B - | ✓ | |
| C | A B C | ✓ | |
| A | A B C | — | hit |
| B | A B C | — | hit |
| D | D B C | ✓ | 替换 A：A 在位置 7 才用，C 在位置 11，B 在位置 9。A 最远→替换 A |
| A | D B A | ✓ | 替换 C：A 是未来(已到)，B 在 9，D 不再用。替换 D？不对——看看未来：B 在 9，C 在 11，A 刚被用已过去了。替换最远的 C。哦不对，现在 frame 是 D B C，要访问 A。A miss→需要换出。D 不再出现，C 在位置 11，B 在位置 9。D 永远不会再用了→**换出 D**。 |
| D | D B A | — | D hit（刚放进去）— 不，D 是在前一帧中吗？不，上一步是 D 被换出换成 A，frame = A B C。现在引用 D，miss。frame = D B C → 换出 A（A 不再出现？A 未来无引用，C 在 11）。换出 A 或 C？A 和 C 都在未来... A 未来无引用（×），C 在 11。A 不再出现 → 换 A。 |
| B | D B C | — | hit |
| C | D B C | — | hit |
| B | D B C | — | hit |

等一下，让我重新仔细做。

**Optimal 详细过程**：

| Ref | Frame 0 | Frame 1 | Frame 2 | Fault |
|:---:|:-------:|:-------:|:-------:|:-----:|
| A | A | — | — | ✓ |
| B | A | B | — | ✓ |
| C | A | B | C | ✓ |
| A | A | B | C | — |
| B | A | B | C | — |
| D | A | B | **D** | ✓（替换 C：A 在位置 7 用，B 在位置 9 用，C 在位置 11 用。C 最远→换 C） |
| A | A | B | D | — |
| D | A | B | D | — |
| B | A | B | D | — |
| C | A | B | **C** | ✓（替换 D：A 不再出现，B 不再出现，D 不再出现。选 D 替换） |
| B | A | B | C | — |

**Optimal: 5 faults**

---

**FIFO 详细过程**：

| Ref | Frame 0 | Frame 1 | Frame 2 | Fault |
|:---:|:-------:|:-------:|:-------:|:-----:|
| A | A | — | — | ✓ |
| B | A | B | — | ✓ |
| C | A | B | C | ✓ |
| A | A | B | C | — |
| B | A | B | C | — |
| D | **D** | B | C | ✓（替换 A：最早进入） |
| A | D | **A** | C | ✓（替换 B：现在最早是 B） |
| D | D | A | C | — |
| B | D | A | **B** | ✓（替换 C：现在最早是 C） |
| C | **C** | A | B | ✓（替换 D：现在最早是 D） |
| B | C | A | B | — |

**FIFO: 7 faults**

---

**LRU 详细过程**：

| Ref | Frame 0 | Frame 1 | Frame 2 | Fault | 访问历史（最近→最远） |
|:---:|:-------:|:-------:|:-------:|:-----:|------|
| A | A | — | — | ✓ | A |
| B | A | B | — | ✓ | B,A |
| C | A | B | C | ✓ | C,B,A |
| A | A | B | C | — | A,C,B |
| B | A | B | C | — | B,A,C |
| D | A | B | **D** | ✓ | D,B,A（替换最远的 C） |
| A | A | B | D | — | A,D,B |
| D | A | B | D | — | D,A,B |
| B | A | B | D | — | B,D,A |
| C | **C** | B | D | ✓ | C,B,D（替换最远的 A） |
| B | C | B | D | — | B,C,D |

**LRU: 5 faults**（在此例中与 Optimal 相同）

---

## 题目 5：Belady's Anomaly

**知识点**：FIFO 的 Belady 异常
**来源**：L06 Demand Paging

### 题目

给定参考串：`A B C D A B E A B C D E`
分别用 **3 个页帧** 和 **4 个页帧** 的 FIFO 算法，计算 Page Fault 次数。观察是否出现 Belady's Anomaly。

### 解答 — 3 页帧 FIFO

| Ref | F0 | F1 | F2 | Fault |
|:---:|:--:|:--:|:--:|:-----:|
| A | A | — | — | ✓ |
| B | A | B | — | ✓ |
| C | A | B | C | ✓ |
| D | D | B | C | ✓ |
| A | D | A | C | ✓ |
| B | D | A | B | ✓ |
| E | E | A | B | ✓ |
| A | E | A | B | — |
| B | E | A | B | — |
| C | E | C | B | ✓ |
| D | E | C | D | ✓ |
| E | E | C | D | — |

**3 页帧 FIFO: 9 faults**

---

### 解答 — 4 页帧 FIFO

| Ref | F0 | F1 | F2 | F3 | Fault |
|:---:|:--:|:--:|:--:|:--:|:-----:|
| A | A | — | — | — | ✓ |
| B | A | B | — | — | ✓ |
| C | A | B | C | — | ✓ |
| D | A | B | C | D | ✓ |
| A | A | B | C | D | — |
| B | A | B | C | D | — |
| E | E | B | C | D | ✓ |
| A | E | A | C | D | ✓ |
| B | E | A | B | D | ✓ |
| C | E | A | B | C | ✓ |
| D | D | A | B | C | ✓ |
| E | D | E | B | C | ✓ |

**4 页帧 FIFO: 10 faults > 9 faults**

**结论**：增加页帧数(3→4)反而增加了 page fault(9→10)，这就是 **Belady's Anomaly**！

---

## 题目 6：Effective Access Time (EAT)

**知识点**：EAT 计算与 page fault 率要求
**来源**：L06 Demand Paging

### 题目

假设内存访问时间 = 200ns，平均 page-fault 服务时间 = 8ms。
1. 若每 1000 次内存访问发生 1 次 page fault，计算 EAT。
2. 若要将性能下降控制在 10% 以内，page fault 率应控制在多少以下？

### 解答

**问题 1**：
```
p = 1/1000 = 0.001
EAT = (1-p) × 200ns + p × 8,000,000ns
    = 0.999 × 200 + 0.001 × 8,000,000
    = 199.8 + 8000
    = 8199.8 ns ≈ 8.2 μs
```
**速度降低** = 8199.8 / 200 ≈ **41 倍！**

---

**问题 2**：
性能下降不超过 10% 意味着：
```
EAT ≤ 200 × 1.1 = 220 ns
(1-p) × 200 + p × 8,000,000 ≤ 220
200 - 200p + 8,000,000p ≤ 220
7,999,800p ≤ 20
p ≤ 2.5 × 10⁻⁶
```
即每 **400,000 次**内存访问最多允许 **1 次** page fault。

---

## 题目 7：页表大小计算

**知识点**：页表大小、多级页表
**来源**：L05 Paging

### 题目

某 32-bit 系统使用 4KB 页面，每个 PTE 占用 4 字节（包含 PFN 和状态位）。
1. 计算单个进程的页表大小（一级页表）。
2. 若 100 个进程运行，总页表开销是多少？
3. 若改用两级页表，L1 index=10bit, L2 index=10bit，L1 页表始终在内存，L2 页表按需在内存。假设进程实际只使用地址空间的最高 2GB（即一半），计算实际页表内存开销。

### 解答

**问题 1**：
```
虚拟地址位数 = 32
Offset 位数 = log₂(4096) = 12
VPN 位数 = 32 - 12 = 20
PTE 数量 = 2²⁰ = 1,048,576
每个 PTE = 4 bytes
页表大小 = 1,048,576 × 4 = 4,194,304 bytes = 4 MB
```

---

**问题 2**：
```
100 × 4MB = 400 MB
```
仅页表就占了 400MB — 不可接受！

---

**问题 3**：
```
L1 PTE 数量 = 2¹⁰ = 1024
L1 页表大小 = 1024 × 4 = 4 KB（正好一页）

L2 页表大小 = 1024 × 4 = 4 KB（每个 L2 页表也是一页）
进程使用高 2GB，即 VPN 的最高 512 个 L2 页表（1024 / 2）
实际开销 = L1(4KB) + 512 × L2(4KB) = 4 + 2048 = 2052 KB ≈ 2 MB

100 个进程 = 100 × 2MB = 200 MB
```
尽管仍然很大，但比 400MB 节省了一半。因为低 2GB 的地址空间未被使用，对应的 L2 页表无需分配。

---

## 题目 8：iNode 最大文件大小

**知识点**：基于 iNode 结构的最大文件大小计算
**来源**：L12 File System

### 题目

某 Unix 文件系统使用以下 iNode 结构：
- 12 个直接指针（direct pointers）
- 1 个单间接指针（single indirect）
- 1 个双间接指针（double indirect）
- 1 个三间接指针（triple indirect）

块大小 = 4KB，块地址长度 = 4 字节。计算该文件系统支持的最大文件大小。

### 解答

```
每个块可存储的指针数 = 4KB / 4B = 1024

直接指针：12 × 4KB = 48 KB

单间接指针：
  1 个间接块 × 1024 指针/块 × 4KB = 1024 × 4KB = 4 MB

双间接指针：
  1 × 1024 × 1024 × 4KB = 1024² × 4KB = 4 GB

三间接指针：
  1 × 1024 × 1024 × 1024 × 4KB = 1024³ × 4KB = 4 TB

最大文件大小 = 48KB + 4MB + 4GB + 4TB ≈ 4 TB
```

**实际中**，4TB 是上限，因为 4KB 块 × 4 字节地址 → 2³² 个块地址 → 2³² × 2¹² = 2⁴⁴ = 16TB。但三间接只到 4TB，加上前面的约 4TB 多一些。

---

## 题目 9：磁盘访问时间

**知识点**：磁盘 Seek Time + Rotational Latency + Transfer Time
**来源**：L11 I/O and Storage

### 题目

某硬盘参数如下：
- 平均寻道时间：5ms
- 转速：7200 RPM
- 传输速率：4 MB/s
- 扇区大小：1 KB

计算：
1. 从磁盘随机位置读取一个扇区的时间。
2. 从同一柱面随机位置读取一个扇区的时间。
3. 从当前磁道的下一个扇区读取的时间。
4. 若文件大小为 1MB，完全顺序存储在同一柱面上，读取整个文件需要多长时间？

### 解答

**问题 1 — 随机位置**：
```
旋转一圈时间 = 60,000 ms / 7200 = 8.33 ms
平均旋转延迟 = 8.33 / 2 ≈ 4 ms
传输时间 = 1KB / (4MB/s) = 1/(4×1024) s = 1/4096 s ≈ 0.26 ms
总时间 = 5 + 4 + 0.26 ≈ 9.26 ms
有效带宽 = 1KB / 9.26ms ≈ 108 KB/s
```

---

**问题 2 — 同柱面**：
```
寻道时间 = 0（已在正确柱面）
总时间 = 0 + 4 + 0.26 ≈ 4.26 ms
有效带宽 = 1KB / 4.26ms ≈ 235 KB/s
```

---

**问题 3 — 同一磁道下一扇区**：
```
寻道时间 = 0
旋转延迟 = 0（刚好在正确位置）
总时间 = 0.26 ms
有效带宽 = 1KB / 0.26ms ≈ 3.85 MB/s ≈ 4 MB/s
```

---

**问题 4 — 1MB 同柱面顺序文件**：
```
需要的扇区数 = 1024 KB / 1KB = 1024 扇区
但不同磁道间需要寻道...
假设文件完全连续地放在相邻磁道上，每磁道约 256 扇区（取决于盘面设计）。
更简单的方法：同柱面意味着磁头不移动(seek=0)
总时间 ≈ 旋转一整圈定位到起始位置(4ms) + 1024 × 0.26ms ≈ 4 + 266 ≈ 270 ms
有效带宽 ≈ 1MB / 0.27s ≈ 3.7 MB/s
```

**关键结论**：磁盘的顺序访问比随机访问快约 **100 倍**。文件系统设计应最大化顺序访问。

---

## 题目 10：银行家算法 — 安全检查

**知识点**：Banker's Algorithm 的 Safety Check 和 Resource Request
**来源**：L10 Deadlock

### 题目

系统有 5 个进程 P0-P4，3 种资源类型 A(10 instances), B(5 instances), C(7 instances)。
在时刻 T0 的状态如下：

| | Allocation | | | Max | | |
|---|:---:|:---:|:---:|---|:---:|:---:|:---:|
| | A | B | C | | A | B | C |
| P0 | 0 | 1 | 0 | | 7 | 5 | 3 |
| P1 | 2 | 0 | 0 | | 3 | 2 | 2 |
| P2 | 3 | 0 | 2 | | 9 | 0 | 2 |
| P3 | 2 | 1 | 1 | | 2 | 2 | 2 |
| P4 | 0 | 0 | 2 | | 4 | 3 | 3 |

Available = (3, 3, 2)

1. 计算 Need 矩阵。
2. 判断系统当前是否处于 Safe State（给出安全序列）。
3. 若 P1 请求 (1, 0, 2)，是否可以批准？
4. 若 P4 请求 (3, 3, 0)，是否可以批准？
5. 若 P0 请求 (0, 2, 0)，是否可以批准？

### 解答

**问题 1 — Need = Max - Allocation**：

| | Need A | Need B | Need C |
|---|:------:|:------:|:------:|
| P0 | 7 | 4 | 3 |
| P1 | 1 | 2 | 2 |
| P2 | 6 | 0 | 0 |
| P3 | 0 | 1 | 1 |
| P4 | 4 | 3 | 1 |

---

**问题 2 — 安全检查**：

Work = Available = (3, 3, 2)；Finish = [F, F, F, F, F]

| 迭代 | 可满足 Need ≤ Work | 选择 | Work += Allocation | Finish |
|:---:|------|:---:|------|:---:|
| 1 | P1(1,2,2)≤(3,3,2) ✓ / P3(0,1,1)≤(3,3,2) ✓ | **P1** | (5,3,2) | [F,T,F,F,F] |
| 2 | P3(0,1,1)≤(5,3,2) ✓ / P4(4,3,1)≤(5,3,2) ✓ | **P3** | (7,4,3) | [F,T,F,T,F] |
| 3 | P4(4,3,1)≤(7,4,3) ✓ / P0(7,4,3)≤(7,4,3) ✓ | **P4** | (7,4,5) | [F,T,F,T,T] |
| 4 | P0(7,4,3)≤(7,4,5) ✓ / P2(6,0,0)≤(7,4,5) ✓ | **P0** | (7,5,5) | [T,T,F,T,T] |
| 5 | P2(6,0,0)≤(7,5,5) ✓ | **P2** | (10,5,7) | [T,T,T,T,T] |

安全序列：**<P1, P3, P4, P0, P2>**

系统处于 **Safe State** ✓

---

**问题 3 — P1 request (1, 0, 2)**：

```
Request = (1,0,2) ≤ Need[P1] = (1,2,2) ✓
Request = (1,0,2) ≤ Available = (3,3,2) ✓
```

假装分配：
```
Available' = (3,3,2) - (1,0,2) = (2,3,0)
Allocation[P1]' = (2,0,0) + (1,0,2) = (3,0,2)
Need[P1]' = (1,2,2) - (1,0,2) = (0,2,0)
```

安全检查（新状态）：
Work = (2,3,0)

| 迭代 | 选择 | Work |
|:---:|:---:|------|
| 1 | P1(0,2,0)≤(2,3,0) | (5,3,2) |
| 2 | P3(0,1,1)≤(5,3,2) | (7,4,3) |
| 3 | P4(4,3,1)≤(7,4,3) | (7,4,5) |
| 4 | P0(7,4,3)≤(7,4,5) | (7,5,5) |
| 5 | P2(6,0,0)≤(7,5,5) | (10,5,7) |

安全序列：**<P1, P3, P4, P0, P2>** → **可以批准** ✓

---

**问题 4 — P4 request (3, 3, 0)**：

```
Request = (3,3,0) ≤ Need[P4] = (4,3,1) ✓
Request = (3,3,0) ≤ Available = (3,3,2)? (3,3,0) ≤ (3,3,2) ✓
```

假装分配：
```
Available' = (3,3,2) - (3,3,0) = (0,0,2)
Allocation[P4]' = (0,0,2) + (3,3,0) = (3,3,2)
Need[P4]' = (4,3,1) - (3,3,0) = (1,0,1)
```

安全检查：Work = (0,0,2)

任何进程的 Need ≤ (0,0,2)？
- P1(1,2,2) ✗ / P3(0,1,1) ✗ / P4(1,0,1) ✗ / P0(7,4,3) ✗ / P2(6,0,0) ✗

**没有一个进程可以满足** → Unsafe → **不能批准** ✗

---

**问题 5 — P0 request (0, 2, 0)**：

```
Request = (0,2,0) ≤ Need[P0] = (7,4,3) ✓
Request = (0,2,0) ≤ Available = (3,3,2) ✓
```

假装分配：
```
Available' = (3,1,2)
Allocation[P0]' = (0,3,0)
Need[P0]' = (7,2,3)
```

安全检查：Work = (3,1,2)

- P1(1,2,2)≤(3,1,2)? B:2≤1? ✗
- P3(0,1,1)≤(3,1,2)? ✓ → Work = (5,2,3)
- P1(1,2,2)≤(5,2,3)? ✓ → Work = (7,2,3)
- P4(4,3,1)≤(7,2,3)? B:3≤2? ✗
- P0(7,2,3)≤(7,2,3)? ✓ → Work = (7,3,3)
- P4(4,3,1)≤(7,3,3)? ✓ → Work = (7,3,5)
- P2(6,0,0)≤(7,3,5)? ✓

安全序列：**<P3, P1, P0, P4, P2>** → **可以批准** ✓

---

## 题目 11：死锁检测算法

**知识点**：多实例资源类型的死锁检测
**来源**：L10 Deadlock

### 题目

系统状态如下：
- 5 个进程 P0-P4
- 3 种资源 A(7 instances), B(2 instances), C(6 instances)

| | Allocation | | | Request | | |
|---|:---:|:---:|:---:|---|:---:|:---:|:---:|
| | A | B | C | | A | B | C |
| P0 | 0 | 1 | 0 | | 0 | 0 | 0 |
| P1 | 2 | 0 | 0 | | 2 | 0 | 2 |
| P2 | 3 | 0 | 3 | | 0 | 0 | 0 |
| P3 | 2 | 1 | 1 | | 1 | 0 | 0 |
| P4 | 0 | 0 | 2 | | 0 | 0 | 2 |

Available = (0, 0, 0)（已全部分配）

1. 存在死锁吗？
2. 若 P2 再请求 1 个 C 类型的实例，Request[P2] = (0, 0, 1)，会发生什么？

### 解答

**问题 1**：

```
Work = Available = (0, 0, 0)
```

找 Requestᵢ ≤ Work：
- P0: (0,0,0) ≤ (0,0,0) ✓ → Work = (0,1,0)
- P2: (0,0,0) ≤ (0,1,0) ✓ → Work = (3,1,3)
- P1: (2,0,2) ≤ (3,1,3) ✓ → Work = (5,1,3)
- P3: (1,0,0) ≤ (5,1,3) ✓ → Work = (7,2,4)
- P4: (0,0,2) ≤ (7,2,4) ✓ → Work = (7,2,6)

全部 Finish = true → **无死锁** ✓

---

**问题 2**：
P2 request (0,0,1) → Request[P2] = (0, 0, 1), Available = (0, 0, 0)

检测：Work = (0, 0, 0)

- P0: (0,0,0) ≤ (0,0,0) ✓ → Work = (0,1,0)
- P2: (0,0,1) ≤ (0,1,0)? C:1≤0? ✗
- P1: (2,0,2) ≤ (0,1,0)? ✗
- P3: (1,0,0) ≤ (0,1,0)? ✗
- P4: (0,0,2) ≤ (0,1,0)? ✗

P1, P2, P3, P4 全部 Finish = false → **死锁存在**！
死锁进程：P1, P2, P3, P4

---

## 知识点与题型速查

| 题型 | 来源 | 关键公式/方法 |
|------|------|------|
| SJF Waiting/Turnaround | L08 | Gantt Chart + 等待时间累加 |
| SRTF (Preemptive SJF) | L08 | 每次新进程到达比较剩余时间 |
| RR 调度 | L08 | Quantum 用完 → 队尾；新进程→队尾 |
| Page Replacement | L06 | Optimal(替换最远未来) / FIFO / LRU |
| Belady's Anomaly | L06 | 仅 FIFO；帧多反而 fault 多 |
| EAT | L06 | (1-p)×HitTime + p×MissPenalty |
| 页表大小 | L05 | 2^(m-k) × PTE_size；多级页表按需分配 |
| iNode 最大文件大小 | L12 | Direct + 1×N + 1×N² + 1×N³；N=BlockSize/AddrLen |
| 磁盘访问时间 | L11 | Seek + RotLat + Transfer；RPM→旋转一圈→半圈 |
| 银行家算法 | L10 | Need=Max-Alloc; Safety Check; Request Check |
| 死锁检测 | L10 | 类似 Safety Check 但用 Request 替代 Need |
| 内存分配策略 | L04 | Best Fit(最小够用) / Worst Fit(最大) / First Fit(第一个) |
