# L02-L13 操作系统知识点详细总结

> 课程：Operating Systems (H), Spring 2026, Prof. Yinqian Zhang
> 格式与 L08 总结一致，按课件章节展开

---

# L02 — OS Basics（操作系统基础）

---

## 一、计算机系统组织

### 1. 冯·诺依曼架构（von Neumann Architecture）
- **单一共享内存** 用于程序和数据
- 单一总线用于内存访问
- 算术单元 + 程序控制单元
- John von Neumann (1903-1957)：现代计算机架构和博弈论之父

### 2. 计算机系统四层结构

| 层 | 说明 |
|---|------|
| Hardware | CPU、内存、I/O 设备，提供基本计算资源 |
| Operating System | 控制和协调硬件使用 |
| Application Programs | 字处理器、编译器、浏览器、数据库、游戏 |
| Users | 人、机器、其他计算机 |

---

## 二、什么是操作系统

### 3. OS 的定义
- 一组软件，使计算机**正确、高效、易于使用**地运行
- **执行用户程序**，使解决问题更容易
- **使计算机系统方便易用**
- **高效使用硬件**（硬件抽象）

### 4. OS 的组成
- **Kernel（内核）**：管理所有物理设备（CPU、RAM、硬盘），通过系统调用提供服务
- **Helper programs**：Shell（CLI）、GUI、Browser 等

### 5. OS 的三大角色
- **Resource Manager**：管理 CPU、内存、磁盘、I/O 设备，仲裁冲突请求
- **Control Program**：控制程序执行，防止错误和不当使用
- **虚拟化提供者**：
  - **CPU 虚拟化**：在单 CPU 上运行多程序（仿佛有多个 CPU）
  - **内存虚拟化**：每个进程错觉独占内存地址空间
  - **并发**：运行多线程程序并确保正确执行
  - **持久化**：将数据写入持久存储

---

## 三、操作系统演进

### 6. OS 发展简史
| 阶段 | 特征 |
|------|------|
| 早期 OS | 仅为处理低级 I/O 的库 |
| Atlas 系统 | 引入系统调用，提升硬件特权级别 |
| UNIX | 支持多道程序和内存保护 |
| PC 时代 | 更好的安全性和可用性（DOS, Mac OS, Windows, Linux） |
| 智能手机 | iOS, Android，面向用户的应用，更多传感器 |

### 7. UNIX 简史
- 受 MIT Multics 系统影响
- Ken Thompson 和 Dennis Ritchie @ Bell Labs
- 支持 Shell 和 Pipe 元编程，用 C 语言编写
- 演化出 BSD（高级虚拟内存、文件系统、网络）
- 商业版本：SunOS, AIX, HPUX, IRIX
- macOS 核心基于 UNIX
- 启发 Linus Torvalds 创建 Linux

---

## 四、双模式操作（Dual-mode Operation）

### 8. Kernel Mode vs. User Mode
- **目的**：允许 OS 保护自身和其他系统组件
- **Mode bit**：由 CPU 硬件提供，区分当前运行的是用户代码还是内核代码
- **特权指令**：只能在 Kernel Mode 执行

### 9. CPU 特权级
| 架构 | 用户态 | 内核态 | 其他 |
|------|--------|--------|------|
| x86 (Intel/AMD) | Ring 3 | Ring 0 | Ring 1, 2 未使用 |
| RISC-V | U-Mode | S-Mode | M-Mode (firmware) |

### 10. 三种模式转换
| 类型 | 触发方式 | 特点 |
|------|----------|------|
| **System Call** | 进程请求系统服务（如 exit） | 类似函数调用但在进程"外部"，通过系统调用号执行 |
| **Interrupt** | 外部异步事件 | 定时器、I/O 设备，独立于用户进程 |
| **Trap/Exception** | 内部同步事件 | 保护违例（段错误）、除零等 |

### 11. System Call 实现机制
- 每个系统调用关联一个**编号**
- 系统调用接口维护一个按编号索引的**表**
- 调用者无需知道实现细节，只需遵守调用约定
- **运行时支持库**（C library）隐藏大部分 OS 接口细节
- 参数传递三种方式：
  - **寄存器**：x86 和 RISC-V 采用
  - **内存块**：参数存于块中，块地址通过寄存器传递
  - **栈**：参数压栈/弹栈

### 12. 系统调用 vs. 库函数
| 库函数 | 是否系统调用？ |
|--------|:------------:|
| `printf()` / `scanf()` | No |
| `malloc()` / `free()` | No |
| `fopen()` / `fclose()` | No（但内部调用 `open()`/`close()`） |
| `mkdir()` / `rmdir()` | Yes |
| `chown()` / `chmod()` | Yes |

---

## 五、内核结构

### 13. Monolithic Kernel（宏内核）
- 拥有访问所有 I/O 设备、内存、硬件中断和 CPU 栈的全部特权
- 包含大量组件：文件系统、设备驱动等
- 基础：Linux、Unix、MS-DOS
- **优点**：性能高（组件间直接通信） | **缺点**：庞大，一个组件崩溃可能拖垮整个系统

### 14. Micro Kernel（微内核）
- 将传统 OS 功能外包给普通用户进程
- 内核中仅保留 IPC、调度器、虚拟内存等最小机制
- 代表：Mach (CMU, 1980s)、seL4（第一个形式化验证的微内核）
- **优点**：响应性更好、更稳定安全（内核代码少）
- **缺点**：更多 IPC → 更多上下文切换 → 更慢

### 15. Hybrid Kernel（混合内核）
- 宏内核和微内核的结合
- 示例：Windows OS

### 16. FrameKernel
- 课件提及 Asterinas 为第一个 FrameKernel 实现

### 17. Hypervisor（虚拟化管理器 / VMM）
- 强调虚拟化和隔离
- OS 可以（几乎）无需修改地运行在 Hypervisor 上
- 资源在虚拟机之间分区

---

## 六、OS 设计原则

### 18. Policy（策略）与 Mechanism（机制）分离
- **Policy**：哪个软件在何时可以访问哪个资源
- **Mechanism**：如何执行策略（请求队列、运行队列、ACL 等）
- **为什么要分离**：策略变化时最大灵活性

### 19. OS 服务
- **用户界面**：CLI、GUI、Batch
- **程序执行**：加载到内存、运行、正常/异常终止
- **I/O 操作**：文件和设备 I/O
- **文件系统操作**：读写、创建删除、搜索、权限管理
- **通信**：共享内存或消息传递
- **错误检测**：CPU、内存、I/O、用户程序错误
- **资源分配**：CPU 周期、内存、文件存储
- **记账**：跟踪用户使用资源情况
- **保护和安全**：控制所有资源访问

---

# L03 — Processes（进程）

---

## 一、进程概念

### 1. 什么是进程
- **进程 = 运行中的程序**
- 程序是磁盘上的文件（代码 + 静态数据）
- 进程由 OS 加载：代码和静态数据从程序加载，堆和栈由 OS 创建

### 2. 进程是机器状态的抽象
- **Memory**：地址空间
- **Register**：PC（程序计数器）、SP（栈指针）、FP（帧指针）
- **I/O**：进程打开的所有文件

### 3. 进程标识：PID
- 每个进程有唯一的**进程ID（PID）**
- `getpid()` 系统调用返回调用进程的 PID

### 4. 进程生命周期状态
- **Running**：正在 CPU 上执行
- **Ready**：准备好运行，等待 CPU
- **Waiting**：等待某个事件（如 I/O 完成）
- **Terminated**：执行完毕

---

## 二、System Call 深入

### 5. System Call = Call by Number
- xv6-riscv 示例：`open()` → `li a7, SYS_open; ecall`
- 内核维护系统调用表：`syscalls[SYS_open] = sys_open`
- `ecall` 指令触发从用户态到内核态的转换

### 6. 参数传递
- **寄存器方式**（x86/RISC-V）：参数放寄存器，参数多时可能不够
- **Block 方式**：参数放内存块，块地址通过寄存器传递
- **栈方式**：参数压栈，OS 弹栈

---

## 三、进程创建：fork()

### 7. fork() 基本行为
- **像细胞分裂**：创建子进程的克隆
- **克隆内容**：
  - PC（从 `fork()` 返回后的同一行代码继续执行）
  - 程序代码（共享同一份代码）
  - 内存（局部变量、全局变量、动态分配内存）
  - 已打开文件
- **不克隆的内容**：
  - `fork()` 返回值（父=子PID，子=0）
  - PID（子进程获新 PID）
  - 运行时间（子进程重置为 0）
  - 文件锁

### 8. fork() 返回值
- **父进程**：返回子进程的 PID（> 0）
- **子进程**：返回 0
- **调度器决定谁先运行**（不确定！）

---

## 四、exec()：执行新程序

### 9. exec() 的行为
- **完全替换**用户空间的所有内容：
  - 程序代码 → 新程序代码
  - 内存（变量、堆）→ 清空
  - 寄存器（包括 PC）→ 重置
- **保留**的内核空间信息：PID、进程关系等
- **`exec()` 之后的代码永远不会被执行**（因为代码已被替换）
- `execl("/bin/ls", "/bin/ls", "-l", NULL)` 的参数顺序：
  1. 要执行的文件路径
  2. `argv[0]`（通常是程序名）
  3. `argv[1]`... 后续参数
  4. NULL 表示参数列表结束

---

## 五、wait() 与 exit()

### 10. wait() 的行为
- 暂停调用进程，直到一个子进程**从运行变为终止**
- **立即返回**（不做任何事）如果：
  - 没有子进程
  - 子进程在父进程调用 `wait()` 之前已终止
- `waitpid()` 可以等待特定子进程，检测不同状态变化

### 11. Shell 的实现模式
```
fork() → 子进程 exec() 目标程序 → 父进程 wait() 子进程终止
```

### 12. exit() 的步骤（内核视角）
1. 清理已分配的内核空间内存（如运行时间信息）
2. 清理进程的用户空间内存
3. 向父进程发送 **SIGCHLD** 信号
4. 进程进入 **Zombie** 状态

### 13. Zombie 进程
- `exit()` 后进程变为 Zombie（已终止但 PCB 仍在进程表中）
- 父进程的 `wait()` 消费 SIGCHLD，真正清理子进程
- **Zombie 占用 PID**，PID 总数有限（`/proc/sys/kernel/pid_max`，通常 32768）
- 用 `ps aux | grep defunct` 查找 Zombie
- Linux 将 Zombie 标记为 `<defunct>`

### 14. wait()+exit() 时序
- **正常情况**：父 `wait()` 阻塞 → 子 `exit()` → 父被唤醒 → 子被清理
- **子先退出**：子变 Zombie 游荡直到父 `wait()` → 父发现已有 SIGCHLD → 立即清理
- **必须清理 Zombie！** 否则 PID 耗尽 → 无法创建新进程

---

## 六、进程层级关系

### 15. 进程树
- **init 进程**：PID = 1，内核启动时创建，运行 `/sbin/init`
- 所有进程都是 init 的后代：`fork()` + `exec()` 创建子进程
- 用 `pstree` 命令查看进程树

### 16. Orphan（孤儿进程）
- 父进程先于子进程终止时，子进程变为孤儿
- **Re-parenting**：Linux 中 init 进程成为所有孤儿的继父
- Windows 维持森林式进程层级（不 re-parent）

### 17. 后台作业（Background Jobs）
- `./program &` 使程序在后台运行
- 依赖 re-parenting 机制：父 shell 退出后，进程被 init 接管

---

## 七、进程时间度量

### 18. User Time vs. Sys Time
| 时间类型 | 含义 |
|----------|------|
| **User time** | 在用户空间代码上花费的 CPU 时间 |
| **Sys time** | 在内核空间代码上花费的 CPU 时间 |
| **Real time** | 程序从开始到结束的实际流逝时间 |

- `real > user + sys`：I/O 密集型
- `real < user + sys`：多线程/多核
- **减少系统调用可大幅加速程序**（课件 `printf` 批量输出 vs 逐次输出示例）

---

## 八、线程（Threads）

### 19. 线程的定义
- 线程是**程序执行的抽象**
- 单线程程序有一个执行点；多线程程序有多个执行点
- 每个线程有私有的：PC、寄存器组、栈（线程局部存储）
- 同一进程的线程共享：地址空间、文件、信号等

### 20. 为什么用线程
- **增加并行性**：每 CPU 一个线程，更好地利用多核
- **避免 I/O 阻塞**：线程允许 I/O 与其他活动重叠
- **资源共享**：同一进程内线程共享地址空间

### 21. 线程实现模型

| 模型 | 映射 | 优点 | 缺点 |
|------|------|------|------|
| **Many-to-One** | 多用户线程→1 内核线程 | 线程间切换开销低 | 一个线程 I/O 阻塞则全部阻塞 |
| **One-to-One** | 1 用户线程→1 内核线程 | 每个线程独立运行/阻塞 | 需进内核态调度 |
| **Many-to-Many** | 多用户线程→多内核线程（≤用户线程数） | 兼具两者优点，灵活 | 实现困难 |

---

## 九、上下文切换

### 22. Context Switch
- OS 重新获得 CPU 控制（通过系统调用/中断/异常）→ 调度器决定下一个进程 → 如果不同，执行上下文切换
- **保存**旧进程状态 → **加载**新进程状态（状态存于 PCB）
- 上下文切换时间 = **系统开销**（不做有用工作）
- **不能太频繁**切换
- **Cooperative scheduling**（早期 Mac）：进程自愿放弃 CPU
- **Non-cooperative scheduling**（现代 OS）：定时器中断强制切换

---

## 十、PCB（Process Control Block）

### 23. PCB 包含的信息
- Process state, PID, runs（运行次数）
- Kernel stack
- `need_resched`（是否需要重新调度）
- Parent pointer
- Memory management (`mm_struct`)
- Context（切换时保存/恢复的寄存器）
- Trapframe（中断时的寄存器状态）
- CR3（页表基址寄存器）
- Flags, name, time_slice
- Files pointer（打开文件信息数组）

### 24. 进程队列
- **Ready Queue**：处于 ready 状态的进程
- **Device Queue**：等待特定 I/O 设备的进程
- PCB 通过链表连接在这些队列中

---

### 25. fork() Copy-on-Write（COW）
- **Case 1**：完全复制地址空间（开销大）
- **Case 2 (COW)**：父子进程共享同一物理页，仅当一方**写入**时才复制
- fork() 时打开的文件数组被复制（包括 stdin/stdout/stderr），所以父子共享终端输出

---

# L04 — Address Translation（地址转换）

---

## 一、多道程序与内存

### 1. 早期 OS
- OS 是一组例程（库），使用低地址内存（物理地址 0 开始）
- 一个运行程序使用其余内存
- **问题**：只有一个程序运行

### 2. 多道程序（Multiprogramming）
- 多个进程同时 ready 运行
- OS 在进程间切换（如一个进程做 I/O 时）
- **好处**：分时共享计算机资源，更有效利用 CPU
- **内存问题**：在磁盘和内存间搬运数据很慢
- **解决方案**：让进程留在内存中，每个进程拥有一小块被划分出的物理内存

### 3. 多道程序内存分区的问题
- Process C 需要更多内存怎么办？
- 如何编译 Program B 使其知道它将运行在 192KB？
- Process C 出错写入 1KB 或 330KB 怎么办？

---

## 二、地址空间抽象

### 4. 地址空间
- **定义**：进程对计算机系统内存的视图
- **段结构**：
  - Code segment（底部）
  - Heap（`malloc`，向上增长）
  - Stack（局部变量、参数、返回值，向下增长）
  - 库映射区域
- **32-bit CPU** 支持最大 4GB 地址空间
- **64-bit CPU** 支持最大 4EB，但实际受限于地址位数（x86-64 仅用 48 bits = 256TB）

### 5. 内存虚拟化
- **Virtual Address**：进程自己地址空间中的地址
- **Physical Address**：物理内存中的地址
- **Address Translation**：虚拟→物理地址转换
- 三大要求：
  - **透明性**（Transparent）：进程不可见
  - **效率性**（Efficient）：时间快、空间省
  - **保护性**（Protection）：一个进程不能访问另一个进程或内核的内存

---

## 三、地址转换机制

### 6. MMU（Memory Management Unit）
- 通常在 CPU 芯片上
- 将指令使用的虚拟地址翻译为 DRAM 理解的物理地址
- CPU 对每次内存访问进行**插接**（Interposition）

### 7. 地址转换四步走
1. CPU 取指：虚拟地址 → MMU 翻译 → 物理地址
2. 指令从物理地址取出
3. CPU 执行指令（如 `ld a1, 0(a0)`）：虚拟地址 → MMU 翻译 → 物理地址
4. 数据从物理地址读入寄存器

---

## 四、Base & Bounds（动态重定位）

### 8. 基本机制
- 两个硬件寄存器：
  - **Base Register**：进程在物理内存中的起始地址
  - **Bounds (Limit) Register**：进程可访问的最大地址范围
- 转换公式：`Physical Address = Virtual Address + Base`
- 保护检查：`Virtual Address < Bounds` ✓（否则引发异常）

### 9. 硬件 & OS 协同

| 硬件支持 | 说明 |
|----------|------|
| Privileged mode | 防止用户进程更新 base/bounds |
| Base/bounds 寄存器 | 每个 CPU 一对 |
| 异常处理寄存器 | 仅 OS 可设置 |

| OS 支持 | 说明 |
|---------|------|
| 内存管理 | 分配/回收内存，维护 free list |
| Base/bounds 管理 | 上下文切换时正确设置 |
| 异常处理 | 终止违规进程 |

### 10. Base & Bounds 的局限
- **内部碎片**：堆和栈之间的空间浪费
- **不能支持更大地址空间**：地址空间等于分配的物理槽
- **难以实现进程间共享**：无法共享代码段或内存

---

## 五、分段（Segmentation）

### 11. 分段机制
- 每个段（code, stack, heap）各有一对 base/bounds 寄存器
- 每个段映射到物理内存的不同区域
- 解决了 Base & Bounds 的部分问题（内部碎片减少、地址空间更大）

### 12. 段表实现
- Base/bounds 组织为表，Segment ID（虚地址高位）索引
- 段寄存器表可存于内存中 → 通过**描述符表寄存器**指向
- **代价**：每次地址转换需额外一次内存读取

### 13. 内存共享与保护
- 代码共享：多个进程使用相同程序/库代码时，段指向相同物理地址
- 保护位扩展：**Read / Write / Execute** 权限

### 14. 分段的问题
- 上下文切换需保存/恢复所有段寄存器对
- 段可能增长（不总是可能）
- **外部碎片**：可变大小段之间的空闲间隙 → 所有空闲空间加起来可能很大但每个太小

---

## 六、空闲空间管理

### 15. 空闲链表（Free List）
- OS 维护所有空闲物理内存区域的链表
- 理想算法既要**快**又要**最小化碎片**

### 16. 三种分配策略

| 策略 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **Best Fit** | 选刚好够用的最小空闲块 | 最小化外部碎片 | 遍历所有空闲块，慢 |
| **Worst Fit** | 选最大的空闲块 | 留下更大的"洞" | 实践中严重碎片，慢 |
| **First Fit** | 选第一个足够大的块 | 快 | 在空闲链表头部产生许多小块 |

**结论**：没有最佳选择，各有权衡。

### 17. 碎片类型
- **内部碎片**：分配的空间大于实际需要的空间（Base & Bounds 中堆栈之间、分段中分配过大段）
- **外部碎片**：空闲块被切成小碎片，没有一个足够大但加起来很大（分段特征）

---

# L05 — Paging（分页）

---

## 一、分页的动机

### 1. 分段问题的解决方案
- 将段表存于内存（解决寄存器数量限制）
- 允许虚拟连续内存物理不连续
- **固定大小**的分段 → 消除外部碎片

### 2. 分页基本概念
- **物理内存**被划分为固定大小的 **Page Frame**（页帧）
- **虚拟地址空间**被划分为同样大小的 **Page**（页）
- Page 映射到 Page Frame
- 典型页大小：**4KB**（1KB-16KB，大多数 ISA 用 4KB）
- **太大** → 内部碎片；**太小** → 页表太大

### 3. 地址结构（32-bit, 4KB page）
```
Virtual Address = VPN (20 bits) || Offset (12 bits)
Physical Address = PFN (20 bits) || Offset (12 bits)
```
- Offset 不变，仅 VPN→PFN 转换
- 地址空间：2³² = 4GB；页大小：2¹² = 4KB；页数：2²⁰ = 1M

---

## 二、页表与 PTE

### 4. Page Table
- 每个进程一个页表，驻留在物理内存中
- 每个条目的索引 = VPN，内容 = PFN + 状态位
- 上下文切换只需改变页表指针（如 CR3 寄存器）

### 5. Page Table Entry (PTE) 的状态位

| 位 | 含义 |
|---|------|
| **Valid bit** | 该虚拟页是否有有效映射（稀疏地址空间很重要） |
| **Protection bits** | Read / Write / Execute 权限 |
| **Present bit** | 该页在物理内存中还是磁盘上 |
| **Dirty bit** | 该页是否被修改过（回写时使用） |
| **Access bit** | 该页是否被访问过（用于页面置换） |

### 6. 页表大小问题
- 假设：32-bit 机器，4KB 页，每个 PTE 4 字节
- 页表条目数：2³² / 2¹² = 2²⁰ = 1M
- 一个页表大小：**4MB**
- 100 个进程：**400MB** — 太大了！

---

## 三、多级页表

### 7. 两级页表（Two-level Page Table）
- 32-bit VA：10-bit L1 index | 10-bit L2 index | 12-bit offset
- **Level 1** 页表始终在内存中
- **Level 2** 页表只为实际使用的虚拟地址范围而存在
- 节省空间的核心思路：大部分虚拟地址空间是**稀疏**的（不需要 L2 页表）

### 8. 树状结构
```
PageTablePtr → L1 PTE → L2 page table (valid & present) → PFN + Offset
                       → L2 page table (valid & non-present, on disk)
                       → invalid (no L2 page table)
```
- L2 页表也是**一个页帧大小**（4KB），包含 1024 个 4 字节 PTE

### 9. 多级页表层次
- 两级（32-bit）、三级（如 SV39）、四级（如 IA-32e/64-bit Linux）

---

## 四、其他页表结构

### 10. Hashed Page Table（哈希页表）
- VPN 通过 **Hash 函数** 映射到哈希表条目
- **冲突处理**：链表（每个元素包含 VPN、PFN、next 指针）

### 11. Inverted Page Table（倒排页表）
- **整个系统一个页表**（而非每个进程一个）
- 每个条目对应一个物理页帧
- 条目包含：PID + VPN → 用于查找
- 需要**线性搜索**整个表来匹配（慢）
- 内存共享困难
- 用于 UltraSPARC 和 PowerPC（64-bit）

---

## 五、实际分页方案

### 12. RISC-V SV39 分页
- 虚拟地址：39 bits；物理地址：56 bits；页大小：4KB
- 三级页表（9 + 9 + 9 + 12 bits）
- PTE 大小：8 bytes（64 bits），包含 D/A/V/G/U/RSW 状态位

### 13. Intel IA-32 分页
- **两阶段地址转换**：Segmentation → Paging
- 虚拟地址：16-bit segment selector + 32-bit offset
- 分段单元产生 linear address → 分页单元产生 physical address
- 两个描述符表：GDT（全局）和 LDT（局部）

---

## 六、TLB（Translation Lookaside Buffer）

### 14. TLB 的作用
- 分页的时间问题：多级页表需要额外的内存引用（3 级页表 = 3 次额外读取）
- **TLB 是硬件缓存**（MMU 的一部分），缓存最近使用的 PTE
- 替换策略：LRU、FIFO、Random

### 15. TLB 命中与缺失
- **TLB Hit**：VPN 在 TLB 中找到 → 直接使用 PFN（快！）
- **TLB Miss**：VPN 不在 TLB 中 → **Page Table Walk**（慢！）
  - TLB Miss 后执行页表遍历，加载 PTE 到 TLB，重试内存访问

### 16. TLB 为什么有效（局部性）
- **空间局部性**：顺序执行的指令、局部变量（栈上）、数组（堆上）往往在同一页
  - 例如访问 a[0]-a[9] 产生 m,h,h,m,h,h,h,m,h,h → 70% 命中率
  - 大页增加命中率
- **时间局部性**：对同一页的访问往往在时间上接近

### 17. 上下文切换与 TLB
- 两个进程可能使用相同的虚拟地址（但映射到不同物理地址！）
- **方案 1**：**Flush TLB**（上下文切换时使所有条目无效 V→I）→ 开销大
- **方案 2**：扩展 TLB 增加 **ASID**（Address Space ID）→ 无需 flush

---

# L06 — Demand Paging（请求分页）

---

## 一、超越物理内存

### 1. 动机
- 64-bit 机器支持高达 4EB 的地址空间
- 应用程序可能使用超过物理内存的空间
- **解决方案**：将地址空间中暂不使用的部分存放到磁盘（更慢但更大）

### 2. 两种方法
| 方法 | 谁负责 | 说明 |
|------|--------|------|
| **Memory Overlays** | 应用程序 | 程序员手动管理数据在内存和磁盘间移动 |
| **Demand Paging** | OS | 虚拟页映射到物理内存或磁盘→进程看到地址空间的抽象 |

---

## 二、交换空间（Swap Space）

### 3. 交换空间
- 磁盘上的分区或文件
- OS 在此处换出/换入内存页
- 按页大小划分为单元，OS 维护每个单元的磁盘地址

---

## 三、请求分页机制

### 4. Demand Paging
- 只在需要时才将页从磁盘加载到内存（**按需加载**）
- 数据以页为单位传输
- 两个可能的磁盘位置：
  - **Swap space**：OS 为临时存储页而创建（栈、堆页）
  - **Program binary files**：代码页仅在执行时加载，只读，永不回写

### 5. 物理内存作为缓存
| 属性 | 值 |
|------|---|
| Block size | 1 page (4KB) |
| Cache organization | Fully Associative（任意磁盘页映射到任意页帧） |
| Replacement policy | LRU, Random, FIFO |
| On a miss | 去下一级（磁盘）填充页 |
| On a write | Write-back（页被驱逐时回写磁盘） |

### 6. Present Bit 与 Page Fault
- **Present Bit = 1**：页在物理内存中 → 正常访问
- **Present Bit = 0**：页在磁盘上 → **Page Fault 异常**

**Page Fault 处理过程**：
1. 在物理内存中找到一个空闲页帧（或触发页面置换）
2. 从磁盘获取页并存入物理内存
3. 更新 PTE（PFN、Present bit = 1）
4. 返回异常，CPU 重新执行出错的指令

### 7. 完整地址转换流程
```
(1) CPU 发出虚拟地址
(2) TLB Hit → 直接获得物理地址
(3) TLB Miss → 查页表
(4) PTE Present=1 → 加载 TLB，重试
(5) PTE Present=0 → Page Fault Exception
(6)-(10) OS Page Fault Handler → 找空闲页帧 → 磁盘读取 → 更新 PTE
(11) 返回用户态，重试
```

---

## 四、页面置换

### 8. 何时触发页面置换
- **主动置换**（Proactive）：始终保留一些空闲页帧
- **Swap Daemon**：后台进程回收页帧
  - **Low watermark**：触发 swap daemon 的阈值
  - **High watermark**：停止回收的阈值
- **被动置换**：Page Fault 时无空闲页帧才触发

### 9. Effective Access Time (EAT)
```
EAT = Hit Rate × Hit Time + Miss Rate × Miss Penalty
EAT = (1-p) × 200ns + p × 8,000,000ns
```
- 若 1/1000 的访问产生 page fault：EAT ≈ 8.2μs，**速度降低 40 倍**
- 若要将速度降低控制在 10% 以内：p < 2.5×10⁻⁶（每 400,000 次访问约 1 次 page fault）

### 10. 三类 Cache Miss
| 类型 | 原因 | 解决方案 |
|------|------|----------|
| **Compulsory** | 页面首次加载 | Prefetching |
| **Capacity** | 内存不足 | 增加 DRAM 或调整进程内存分配 |
| **Conflict** | 直接映射/组相联冲突 | OS 页缓存是全相联 → 无此问题 |

---

## 五、页面置换策略

### 11. Optimal (MIN)
- 替换**未来最长时间内不会被使用**的页
- 理论上导致最少 page fault
- **不可实现**（需要预知未来）

### 12. FIFO
- 扔掉最早进入的页
- 可能扔掉频繁使用的页

### 13. RANDOM
- 随机选择 → 难以做实时保证

### 14. LRU（Least Recently Used）
- 替换**最长时间未被使用**的页
- 利用**时间局部性**：很久没用的页近期也不太可能被用
- LRU 通常接近 Optimal，但不总是（如循环访问 A B C D）

### 15. LFU（Least Frequently Used）
- 替换访问次数最少的页
- 利用**空间局部性**

### 16. Belady's Anomaly（Belady 异常）
- **增加页帧数量反而导致更多 page fault！**
- 仅发生在 **FIFO** 算法（LRU 和 MIN 不存在此问题）

### 17. LRU 的实现困难
- 需要硬件支持（每次内存访问更新时戳）
- 每次内存访问需额外一次内存写入
- 扫描全部内存找 LRU 页很慢（4GB = 100 万页帧）

---

## 六、LRU 近似算法

### 18. Reference Bit
- 每个页帧一个 Reference bit
- 初始全为 0；第一次被访问时由 CPU 置 1
- 将访问历史近似为两簇：used vs. unused

### 19. Clock Algorithm（时钟算法 / 二次机会）
- 物理页排列成环形链表
- OS 维护一个指针
- 置换时检查当前页的 reference bit：
  - **1**：最近访问过 → 清 0，移到下一页
  - **0**：最近未访问 → 替换候选

### 20. Enhanced Clock Algorithm（带 Dirty Bit）
- 考虑 (reference bit, dirty bit) 两个维度
- 选择优先级（从高到低）：
  1. (0, 0) — 最近未用且未修改 → **最佳替换**
  2. (0, 1) — 最近未用但已修改 → 需先写回
  3. (1, 0) — 最近使用但干净 → 可能很快再用
  4. (1, 1) — 最近使用且已修改 → 可能需要再写回

### 21. Additional-reference-bits Algorithm
- 每个页帧一个 8-bit 历史寄存器
- 每 100ms 定时器中断：reference bit 移入历史寄存器的最高位
- 比较 unsigned integer 值：越大 = 越近使用
- 更多 bits + 更频繁的中断 = 更精确的 LRU 近似

### 22. Nth-chance Clock Algorithm
- 每页帧有 reference bit + counter
- CPU 置 reference bit；OS 检查：
  - 1：清 reference bit 和 counter
  - 0：counter++；若 counter == N → 替换
- N 越大 → 越接近 LRU；N 越小 → 更高效

---

## 七、页帧分配

### 23. 每个进程最少/最多页帧数
- **最少**：取决于架构。一次指令可能需要的页数
  - x86：至少 1 个指令页 + 1 个数据页 + 一些页表页
- **最多**：取决于可用物理内存

### 24. Global vs. Local Replacement
| 策略 | 替换范围 | 特点 |
|------|----------|------|
| **Global** | 所有进程的页帧 | 一个进程可从另一进程夺取帧 |
| **Local** | 只从本进程分配的帧中 | 一个进程不影响其他进程 |

### 25. 分配算法
| 算法 | 公式 | 说明 |
|------|------|------|
| **Equal Allocation** | m / n | 每个进程等量 |
| **Proportional Allocation** | aᵢ = sᵢ / Σsⱼ × m | 按进程大小比例 |
| **Priority Allocation** | 按优先级 | 可按优先级从低优先级进程夺帧 |

### 26. Thrashing（抖动）
- 运行的进程集合的内存需求**超过可用物理内存**
- 早期 OS：计算工作集，减少进程数使之适合内存
- 现代 OS：**Out-of-Memory Killer** 在内存过度承诺时杀进程

---

# L07 — Linux Memory Management（Linux 内存管理）

---

## 一、地址空间划分

### 1. 32-bit Linux 地址空间
- **User space**：0x00000000 → 0xBFFFFFFF（低 3GB）
  - Page 0 无效（检测 NULL 指针）
- **Kernel space**：0xC0000000 → 0xFFFFFFFF（高 1GB）
- 64-bit Linux 类似但分割点不同

### 2. 为什么内核空间映射到每个进程的地址空间？
- 陷入内核时**无需切换页表**（不用切换 CR3）→ **无需 TLB Flush**
- 内核代码可以访问用户内存
- 每个地址空间中的内核映射是**相同的**

### 3. 内核地址类型
| 类型 | 用途 | 特点 |
|------|------|------|
| **Kernel Logical Addresses** | 页表、内核栈、`kmalloc()` | 从 0xC0000000 开始，映射到连续的物理地址 0x00000000，**永不换出** |
| **Kernel Virtual Addresses** | `vmalloc()` | 虚拟连续但物理可能不连续 |

### 4. 内核保护
- 通过页表权限位保护内核空间免遭用户进程访问
- **U-bit**：U=1 才能被用户模式代码访问（内核页 U=0）

---

## 二、大页支持（Huge Pages）

### 5. x86 大页
| 页大小 | 对齐要求 | 优点 |
|--------|----------|------|
| 4KB | 低 12 位为 0 | 默认 |
| 2MB | 低 21 位为 0 | TLB Miss 少 |
| 1GB | 低 30 位为 0 | TLB Miss 更少 |

- Linux 也支持 huge pages
- **优点**：减少 TLB miss；应用可能需要连续物理内存
- **缺点**：内部碎片

### 6. 不同页大小使用不同级数的页表
- 4KB：4 级（L4→L3→L2→L1→Offset）
- 2MB：3 级（L4→L3→L2→Offset，跳过 L1）
- 1GB：2 级（L4→L3→Offset，跳过 L2 和 L1）

---

## 三、Linux 物理内存管理

### 7. 内存分配层次
```
用户态: malloc() → mmap() / brk()
内核态: vmalloc() → 物理不连续的内核虚拟内存
        kmalloc() → 物理连续的内核逻辑地址
```

---

## 四、Slab 分配器

### 8. Slab 概念
- **Slab**：一个或多个物理连续的页
- **Cache**：一个或多个 Slab（每种内核对象类型一个 cache）
- 每个对象大小固定（如 3KB 对象、7.8KB 对象）

### 9. Slab 状态
- **Empty**：所有对象空闲
- **Partial**：部分对象空闲
- **Full**：所有对象已使用
- 请求优先从 Partial → Empty → 从 Buddy System 分配新 Slab

### 10. Slab 优点
- **无碎片浪费**：返回精确所需大小
- **请求快速满足**：对象提前创建和初始化，释放后标记为 free 即可复用

---

## 五、Buddy System（伙伴系统）

### 11. 基本机制
- 空闲物理内存视为大小为 **2^N 页**的大空间
- **分配**：不断对半分割直到找到刚好够大的块
- **释放**：释放的块与其 buddy 递归合并
- **两个 buddy 的物理地址仅 1 bit 不同**
- 分配页 A（3KB），分配页 C+D（5KB），释放页 A，A 与 B 合并

---

## 六、Page Cache（页缓存）

### 12. Page Cache
- 物理内存的一个区域，持有磁盘上的数据
- **Memory-mapped files**：所有二进制和动态库
- **Anonymous memory**：存储在 swap space 中的栈和堆
- 跟踪 clean/dirty，**pdflush** 定期回写脏页

### 13. 2Q 置换策略
- **两个列表**：Inactive list + Active list（LRU 队列）
- 首次访问 → Inactive list
- 再次访问 → 提升到 Active list
- **替换发生在 Inactive list**
- 定期将 Active list 底部页面移到 Inactive list，保持 Active 为 Page Cache 的 2/3
- 使用 **Clock Algorithm** 近似 LRU

---

## 七、安全特性

### 14. DEP（Data Execution Prevention）
- 防止数据页被执行
- 页要么可写要么可执行（PTE 中的 **NX bit**）
- 栈不可执行 → 缓解缓冲区溢出攻击

### 15. ASLR（Address Space Layout Randomization）
- 随机化栈、堆、库的虚拟地址
- 缓解 Return-to-libc 和 ROP 攻击
- libc 和栈/堆地址不为人知 → 增加攻击难度

---

# L08 — CPU Scheduling（CPU 调度）

> 详见 L08 完整总结。此处保留关键知识点索引（避免重复内容）
  L08 CPU Scheduling — 全部知识点

  ---
  一、进程类型（Process Types）

  1. CPU-bound Process（CPU 密集型进程）

  - 定义：大部分运行时间花在 CPU 上，即 user-time > sys-time
  - 例子：AI 课程作业（大量计算）

  2. I/O-bound Process（I/O 密集型进程）

  - 定义：大部分运行时间花在 I/O 上，即 sys-time > user-time
  - 例子：/bin/ls、网络程序

  3. CPU Burst（CPU 突发）

  - 进程执行由 CPU 执行期 和 I/O 等待期 交替组成
  - CPU burst 分布的典型特征：多数 burst 很短，少数 burst 很长（长尾分布）

  ---
  二、CPU Scheduler（CPU 调度器）

  4. 调度器的定义

  - 当多个进程争抢单个 CPU 时，调度器决定下一个运行哪个进程

  5. 调度决策发生的四种时机

  ┌──────┬───────────────────┬────────────────────────────┐
  │ 时机 │     状态转换      │            说明            │
  ├──────┼───────────────────┼────────────────────────────┤
  │ 1    │ Running → Waiting │ 进程发起 I/O 或被阻塞      │
  ├──────┼───────────────────┼────────────────────────────┤
  │ 2    │ Running → Ready   │ 当前进程被抢占             │
  ├──────┼───────────────────┼────────────────────────────┤
  │ 3    │ Waiting → Ready   │ I/O 完成，进程回到就绪队列 │
  ├──────┼───────────────────┼────────────────────────────┤
  │ 4    │ Terminates        │ 进程结束                   │
  └──────┴───────────────────┴────────────────────────────┘

  6. 非抢占式调度（Non-preemptive）

  - 调度仅发生在时机 1 和 4
  - 进程一旦获得 CPU，就一直运行到主动放弃（阻塞或终止）
  - 进程不会被强制剥夺 CPU

  7. 抢占式调度（Preemptive）

  - 调度发生在时机 1、2、3、4 全部情况
  - 调度器可以在任意时刻强制剥夺当前进程的 CPU
  - 代价：更多的上下文切换开销

  ---
  三、调度算法优化指标

  给定一组进程，每个进程有两个参数：
  - Arrival Time：进程到达就绪队列的时间
  - CPU Requirement：预期的 CPU burst 时间

  8. Turnaround Time（周转时间）

  - 定义：从任务到达系统到任务完成（阻塞或终止）之间的时间
  - 公式：Turnaround Time = Completion Time - Arrival Time
  - 目标：最小化平均周转时间

  9. Waiting Time（等待时间）

  - 定义：任务在就绪队列中等待的累计时间
  - 目标：最小化平均等待时间

  10. Context Switches（上下文切换次数）

  - 目标：减少上下文切换次数
  - 每次切换有开销（保存/恢复寄存器、TLB flush 等）

  ---
  四、Shortest-Job-First（SJF）调度算法

  11. Non-preemptive SJF（非抢占式 SJF）

  - 核心思想：在所有已到达的进程中，选择 CPU burst 时间最短的进程运行
  - 当前进程运行期间，即使有更短的新进程到达，也不会抢占
  - 平局时使用 FIFO 打破平局

  课件例题（数据）：

  ┌──────┬─────────┬───────────────┐
  │ Task │ Arrival │ CPU Time Req. │
  ├──────┼─────────┼───────────────┤
  │ P1   │ 0       │ 7             │
  ├──────┼─────────┼───────────────┤
  │ P2   │ 2       │ 4             │
  ├──────┼─────────┼───────────────┤
  │ P3   │ 4       │ 1             │
  ├──────┼─────────┼───────────────┤
  │ P4   │ 5       │ 4             │
  └──────┴─────────┴───────────────┘

  非抢占式 SJF 的执行过程：
  - t=0：只有 P1，P1 运行 7 个单位（t=0 到 t=7）
  - t=7：P1 完成。此时 P2(4)、P3(1)、P4(4) 都已到达。选最短的 P3。
  - t=7-8：P3 运行 1 个单位，P3 完成。
  - t=8-12：P2 和 P4 都需 4 个单位，FIFO 平局，P2 先运行。P2 运行 4 个单位。
  - t=12-16：P4 运行 4 个单位。

  非抢占式 SJF 计算结果：

  ┌─────────────────┬─────┬─────┬─────┬─────┐
  │                 │ P1  │ P2  │ P3  │ P4  │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Waiting Time    │ 0   │ 6   │ 3   │ 7   │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Turnaround Time │ 7   │ 10  │ 4   │ 11  │
  └─────────────────┴─────┴─────┴─────┴─────┘

  - Average Waiting Time = (0+6+3+7)/4 = 4
  - Average Turnaround Time = (7+10+4+11)/4 = 8
  - Context Switches = 3

  12. Preemptive SJF / SRTF（抢占式 SJF / 最短剩余时间优先）

  - 核心思想：每当一个新进程到达就绪队列（不管是从等待状态还是新创建），调度器立即介入，选择剩余 CPU 时间最短的进程运行
  - 如果新到达的进程比当前运行进程的剩余时间更短，就抢占

  抢占式 SJF 执行过程（同样数据）：

  ┌─────────┬──────────────────────────────────────────────────────────────────────────────────────────┐
  │  Time   │                                           事件                                           │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=0     │ P1 开始运行（剩余 7）                                                                    │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=2     │ P2 到达，剩余 4 < P1 剩余 5 → P2 抢占 P1                                                 │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=4     │ P3 到达，剩余 1 < P2 剩余 2 → P3 抢占 P2                                                 │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=5     │ P3 完成（1 单位）。同时 P4 到达，剩余 4。比较：P1 剩 5，P2 剩 2，P4 剩 4 → P2 最短，运行 │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=5-7   │ P2 运行剩余 2 个单位，P2 完成                                                            │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=7-11  │ P4 运行 4 个单位，P4 完成                                                                │
  ├─────────┼──────────────────────────────────────────────────────────────────────────────────────────┤
  │ t=11-16 │ P1 运行剩余 5 个单位，P1 完成                                                            │
  └─────────┴──────────────────────────────────────────────────────────────────────────────────────────┘

  抢占式 SJF 计算结果：

  ┌─────────────────┬─────┬─────┬─────┬─────┐
  │                 │ P1  │ P2  │ P3  │ P4  │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Waiting Time    │ 9   │ 1   │ 0   │ 2   │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Turnaround Time │ 16  │ 5   │ 1   │ 6   │
  └─────────────────┴─────┴─────┴─────┴─────┘

  - Average Waiting Time = (9+1+0+2)/4 = 3
  - Average Turnaround Time = (16+5+1+6)/4 = 7
  - Context Switches = 5

  13. SJF 两种模式对比总结

  ┌─────────────────────┬────────────────────┬────────────────┐
  │        指标         │ Non-preemptive SJF │ Preemptive SJF │
  ├─────────────────────┼────────────────────┼────────────────┤
  │ Avg Waiting Time    │ 4                  │ 3 ✓ (更小)     │
  ├─────────────────────┼────────────────────┼────────────────┤
  │ Avg Turnaround Time │ 8                  │ 7 ✓ (更小)     │
  ├─────────────────────┼────────────────────┼────────────────┤
  │ Context Switches    │ 3                  │ 5 ✗ (更多)     │
  └─────────────────────┴────────────────────┴────────────────┘

  关键结论：抢占式以更多的上下文切换为代价，换取了更短的等待时间和周转时间。

  ---
  五、Round Robin（RR）轮转调度

  14. RR 的基本机制

  - RR 是抢占式调度
  - 每个进程被分配一个 quantum（时间片）
  - 当进程的 quantum 用完（变为 0）时，进程被抢占，放到就绪队列末尾，quantum 重新充满
  - 调度器选择队列中下一个 quantum 非零的进程
  - 进程循环轮流执行，像一个环形队列
  - 新进程到达时不会触发新的调度决策（这点与抢占式 SJF 不同！），新进程只是被加入队尾

  15. RR 的执行过程（Quantum = 3，相同数据）

  ┌──────┬────────────────────────────────────────────────────┐
  │ Time │                        事件                        │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=0  │ P1 运行，Q=3                                       │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=2  │ P2 到达，加入就绪队列（不抢占！P1 继续）           │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=3  │ P1 用完 Q，P1→队尾(Q=3)。P2 出队运行(Q=3)          │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=4  │ P3 到达，加入队尾                                  │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=5  │ P4 到达，加入队尾（现在队列：P1, P3, P4）          │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=6  │ P2 用完 Q，P2→队尾(Q=3)。P1 出队运行(Q=3)          │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=9  │ P1 用完 Q（剩余1），P1→队尾(Q=3)。P3 出队运行(Q=3) │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=10 │ P3 完成（只需1单位）。P4 出队运行(Q=3)             │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=13 │ P4 用完 Q（剩余1），P4→队尾(Q=3)。P2 出队运行(Q=3) │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=14 │ P2 完成（剩余1）。P1 出队运行(Q=3)                 │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=15 │ P1 完成（剩余1）。P4 出队运行(Q=3)                 │
  ├──────┼────────────────────────────────────────────────────┤
  │ t=16 │ P4 完成（剩余1）。全部完成                         │
  └──────┴────────────────────────────────────────────────────┘

  RR 计算结果：

  ┌─────────────────┬─────┬─────┬─────┬─────┐
  │                 │ P1  │ P2  │ P3  │ P4  │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Waiting Time    │ 8   │ 8   │ 5   │ 7   │
  ├─────────────────┼─────┼─────┼─────┼─────┤
  │ Turnaround Time │ 15  │ 12  │ 6   │ 11  │
  └─────────────────┴─────┴─────┴─────┴─────┘

  - Average Waiting Time = (8+8+5+7)/4 = 7
  - Average Turnaround Time = (15+12+6+11)/4 = 11
  - Context Switches = 7

  16. RR vs SJF 全面对比

  ┌─────────────────────┬────────────────────┬────────────────┬──────────┐
  │        指标         │ Non-preemptive SJF │ Preemptive SJF │ RR (Q=3) │
  ├─────────────────────┼────────────────────┼────────────────┼──────────┤
  │ Avg Waiting Time    │ 4                  │ 3 ✓            │ 7 ✗      │
  ├─────────────────────┼────────────────────┼────────────────┼──────────┤
  │ Avg Turnaround Time │ 8                  │ 7 ✓            │ 11 ✗     │
  ├─────────────────────┼────────────────────┼────────────────┼──────────┤
  │ Context Switches    │ 3                  │ 5              │ 7 ✗      │
  └─────────────────────┴────────────────────┴────────────────┴──────────┘

  RR 在所有指标上都是最差的——那为什么还要用它？

  答案：响应性（Responsiveness）。RR 保证了每个进程定期获得
  CPU，用户不会感觉到某个任务"卡死"。这在交互式系统中至关重要。SJF 虽然指标更好，但可能导致长任务长时间得不到执行。

  ---
  六、Priority Scheduling（优先级调度）

  17. 基本机制

  - 每个进程关联一个优先级数值（整数）
  - CPU 分配给优先级最高的进程（数字越小，优先级越高）
  - Non-preemptive：新到达进程仅放入队列，不抢占
  - Preemptive：如果新到达进程优先级高于当前运行进程 → 抢占 CPU

  18. 静态优先级 vs 动态优先级

  - Static Priority：进程整个生命周期中优先级不变
  - Dynamic Priority：优先级随时间变化

  19. SJF 与优先级调度的关系

  - SJF 本质上是优先级调度的一种特例：优先级 = 下一次 CPU burst 时间（burst 越短，优先级越高）

  20. Starvation（饥饿）问题

  - 定义：低优先级进程可能永远得不到执行
  - 经典传闻：MIT 在 1973 年关闭 IBM 7094 时，发现一个 1967 年提交的低优先级进程从未被执行过

  21. Aging（老化）— 解决饥饿的方案

  - 定义：随着时间推移，逐步提高等待进程的优先级
  - 例子：优先级范围 127（最低）到 0（最高），每 15 分钟将等待进程的优先级提高 1 → 从 127 升到 0 需要 32 小时

  ---
  七、Linux 调度器演进

  22. 三个阶段的调度器

  ┌──────────┬───────────────────────┬────────────────────────────────────────────┬──────────────────────────────────┐
  │   版本   │        调度器         │                    特点                    │               缺陷               │
  ├──────────┼───────────────────────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ < 2.5    │ 传统 UNIX 调度        │ 简单                                       │ 不能很好地支持 SMP（对称多处理） │
  ├──────────┼───────────────────────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ ≥ 2.5    │ O(1) 调度器           │ 调度时间恒定（与任务数无关），更好支持 SMP │ 交互式进程响应时间差             │
  ├──────────┼───────────────────────┼────────────────────────────────────────────┼──────────────────────────────────┤
  │ ≥ 2.6.23 │ CFS（完全公平调度器） │ 当前默认调度器                             │ —                                │
  └──────────┴───────────────────────┴────────────────────────────────────────────┴──────────────────────────────────┘

  ---
  八、CFS（Completely Fair Scheduler）— 重点

  23. 调度类（Scheduling Classes）

  Linux 内核实现两类调度：
  1. 默认调度类：CFS（用于普通进程）
  2. 实时调度类：用于实时进程

  24. 可变长度调度 Quantum

  - 传统 UNIX：固定 90ms quantum
  - CFS：按比例分配 CPU 处理时间，而非固定时间片

  25. Nice Value

  - 范围：-20 到 +19，默认值 0
  - Nice 值越小 → 相对优先级越高
  - Nice 值越大 = 越"nice"（谦让），得到的 CPU 时间越少
  - Nice 值越小的任务获得更高比例的 CPU 处理时间

  26. Virtual Runtime（虚拟运行时间，vruntime）

  - 每个任务有一个 vruntime 变量
  - 衰减因子（Decay Factor）：
    - 低优先级（Nice > 0）：衰减速率更高 → 运行 200ms 物理时间，vruntime 大于 200ms
    - 高优先级（Nice < 0）：衰减速率更低 → 运行 200ms 物理时间，vruntime 小于 200ms
    - Nice = 0：vruntime 等于实际物理运行时间
  - 决策规则：调度器选择 vruntime 最小的任务运行
    - vruntime 越小 → 意味着该任务获得的 CPU 时间越"少"→ 优先级越高
  - 抢占：高优先级任务可以抢占低优先级任务

  27. CFS 对 I/O-bound vs CPU-bound 的处理

  - 两个任务有相同 nice 值
  - 一个 I/O-bound，一个 CPU-bound
  - I/O-bound 任务大部分时间在等待 I/O → 实际运行时间少 → vruntime 更短
  - 因此 I/O-bound 任务有更高的实际优先级
  - 当 I/O-bound 任务就绪时，会抢占 CPU-bound 任务

  ▎ 这正是 CFS 设计的巧妙之处：自动给予 I/O 密集型（交互式）进程更高响应优先级，无需手动调整。


### 核心知识点速查
- CPU-bound vs I/O-bound 进程
- 非抢占式（时机 1 & 4）vs 抢占式（时机 1-4）
- Turnaround Time / Waiting Time / Context Switches
- SJF / SRTF（抢占式 SJF）：计算题重点
- Round Robin（Quantum）：计算题重点
- SJF vs RR 对比：为什么 RR 指标最差但还要用？（响应性）
- Priority Scheduling：Starvation → Aging 解决
- Linux 调度演进：传统 UNIX → O(1) → CFS
- CFS：Nice Value（-20 到 +19）、Virtual Runtime（vruntime）、衰减因子

---

# L09 — Synchronization（同步）

---

## 一、竞态条件（Race Condition）

### 1. 问题场景
- 共享内存 X = 10
- Process A：add 10 to X（load→add→write）
- Process B：minus 10 from X（load→minus→write）
- 正常情况下最终 X = 10

### 2. 异常执行（Race Condition 发生）
- A load X=10 → **上下文切换**
- B load X=10 → B minus 10 → B write X=0 → **上下文切换**
- A add 10（寄存器中已是 10+10=20） → A write X=**20**
- **结果应该是 10，却变成了 20！BUG！**

### 3. Race Condition 定义
- 当"共享对象" + "多个进程/线程" + "并发"时可能发生
- **执行结果取决于对共享资源访问的特定顺序**
- 99% 执行正常，1% 出问题 —— 调试噩梦

---

## 二、互斥（Mutual Exclusion）与临界区（Critical Section）

### 4. 互斥解决方案
- 当一个进程访问共享内存时，任何其他进程都不能访问它
- 访问共享对象必须**逐个进行**

### 5. 临界区（Critical Section）
- **定义**：访问共享对象的代码段
- 结构：Section entry（声明开始）→ 访问共享对象 → Section exit（声明结束）
- 临界区应该**尽可能紧凑**（太大会阻塞其他进程）

### 6. 临界区实现的三个要求
| 要求 | 说明 |
|------|------|
| **Mutual Exclusion** | 没有两个进程能同时进入各自的临界区 |
| **Bounded Waiting** | 一个进程开始尝试进入临界区后，其他进程进入的次数有上限 |
| **Progress** | 没有进程在临界区内时，尝试进入的进程之一最终能进入 |

---

## 三、实现方案

### 7. 禁用中断（Disabling Interrupts）
- 单核：正确但不可在用户级使用（若无限循环则 OS 无法恢复控制）
- 多核：**不正确**（除非在所有核上禁用中断！）

### 8. 基于自旋的锁（Spin-based Locks）
- 在共享变量 `turn` 上循环检测其他进程状态
- 基本版本：严格交替 → **Progress 违规**（一方在剩余区慢，另一方无法进入 CS）
- 适用场景：等待时间 < 上下文切换开销
- 多核尤其适用（不会过多阻塞其他无关进程）

### 9. Peterson's Solution（改进的自旋锁）
```c
int turn;
int interested[2] = {FALSE, FALSE};

void lock(int process) {
    int other = 1 - process;
    interested[process] = TRUE;
    turn = other;                       // 礼让对方先走
    while (turn == other && interested[other] == TRUE)
        ;                               // busy waiting
}

void unlock(int process) {
    interested[process] = FALSE;
}
```
- 满足 Mutual Exclusion、Progress、Bounded-waiting 三个要求
- `interested[process]=TRUE` + `turn=other` + 检查对方是否 interested

### 10. 多进程互斥（Multi-Process）
- 使用 `test_and_set()` 原子指令
- 支持 n 个进程的 bounded-waiting 互斥

### 11. Priority Inversion（优先级反转）
- 低优先级进程 L 在临界区内 → 高优先级进程 H 想进入临界区
- H 自旋等待 L 释放锁 → 但 L 因低优先级不被调度 → H 永远自旋
- 现代 OS 通过**优先级继承**解决

---

## 四、信号量（Semaphore）

### 12. Semaphore 结构
```c
typedef struct {
    int value;
    list process_id;   // wait-list
} semaphore;
```

### 13. sem_wait() 和 sem_post()
```c
void sem_wait(semaphore *s) {
    s->value = s->value - 1;
    if (s->value < 0) sleep();    // 睡眠等待
}

void sem_post(semaphore *s) {
    s->value = s->value + 1;
    if (s->value <= 0) wakeup();  // 唤醒等待者
}
```
- `s->value = 1`（初始值）：**二元信号量**（binary semaphore）= 互斥锁
- `s->value > 1`：**计数信号量**（counting semaphore）= 资源计数
- 当 `value < 0` 时，|value| = 等待的进程数

### 14. sem_wait/sem_post 本身的原子性保证
- 单处理器：禁用中断
- 多核：使用原子指令（如 `cmp_xchg`）

---

## 五、经典同步问题

### 15. Producer-Consumer Problem（生产者-消费者）
**设置**：
```c
semaphore mutex = 1;    // 缓冲区互斥
semaphore avail = N;    // 可用空槽数
semaphore fill = 0;     // 已占用槽数
```

**Producer**：
```c
wait(&avail);   // 等待空槽
wait(&mutex);   // 进入临界区
insert_item(item);
post(&mutex);   // 退出临界区
post(&fill);    // 通知消费者
```

**Consumer**：
```c
wait(&fill);    // 等待有数据
wait(&mutex);   // 进入临界区
item = remove_item();
post(&mutex);   // 退出临界区
post(&avail);   // 通知生产者有空槽
```

**关键陷阱**：
- ❌ 去掉 `fill`：消费者可能从空缓冲区取数据 → 错误
- ❌ 交换 `mutex` 和 `avail` 的顺序（先 wait mutex 再 wait avail）→ **死锁**！
  - Producer 持有 mutex 等待 avail（消费者释放），Consumer 等待 mutex（生产者释放）→ 互锁

### 16. Dining Philosophers Problem（哲学家进餐）
**设置**：5 个哲学家，5 根筷子，需要 2 根才能吃饭

**简单方案的问题**：每个哲学家先拿左边再拿右边 → **死锁**（所有人同时拿左边）
**改进方案的问题**：拿不到第二根就放下 → **饥饿**（所有人同步拿起放下循环）

**最终方案**：用信号量建模**哲学家**而非筷子
- `state[N]`：THINKING / HUNGRY / EATING
- `mutex`：保护 state 数组的互斥
- `p[N]`：每个哲学家一个信号量

**`captain(i)` 函数**：
```c
if (state[i] == HUNGRY && state[LEFT] != EATING && state[RIGHT] != EATING) {
    state[i] = EATING;
    post(&p[i]);  // 唤醒哲学家
}
```
- 只有当**左右邻居都不在吃**时才能开始吃
- `take_chopsticks`：设置 HUNGRY → 调用 captain → 若不能吃则 `wait(&p[i])`
- `put_chopsticks`：设置 THINKING → 通知左右邻居（captain(LEFT), captain(RIGHT)）

---

# L10 — Deadlock（死锁）

---

## 一、Starvation vs. Deadlock

### 1. 区别
| | Starvation | Deadlock |
|---|-----------|----------|
| 定义 | 线程无限等待 | 循环等待资源 |
| 能否自行结束 | 可以（但不一定） | 不能（需要外部干预） |
| 关系 | Deadlock → Starvation | Starvation ⇏ Deadlock |

---

## 二、死锁的四个必要条件

### 2. 四个条件（必须全部满足才会死锁）

| 条件 | 说明 |
|------|------|
| **Mutual Exclusion** | 资源一次只能被一个线程使用 |
| **Hold and Wait** | 线程持有至少一个资源，同时等待获取其他线程持有的资源 |
| **No Preemption** | 资源只能由持有者自愿释放（不能被抢占） |
| **Circular Wait** | 存在等待环 T₁→T₂→...→Tₙ→T₁ |

---

## 三、Resource-Allocation Graph（资源分配图）

### 3. 图的要素
- **Thread** (T) 和 **Resource** (R) 两类节点
- **Request edge**：T → R（线程请求资源）
- **Assignment edge**：R → T（资源分配给线程）
- R 内的点数 = 该类型资源的实例数

### 4. 死锁判定
- **图中有环** → 如果每种资源只有一个实例，则必然死锁
- **有环但无死锁**：如果某种资源有多个实例（环可能被外部释放打破）

---

## 四、死锁处理方法

### 5. 三种策略
| 策略 | 做法 | 谁用 |
|------|------|------|
| **Detection & Recovery** | 允许进入死锁，检测后恢复 | 数据库 |
| **Prevention** | 破坏四个条件之一 | 理论 |
| **Ignore** | 假装死锁不存在 | **大多数 OS，包括 UNIX** |

### 6. 死锁检测算法（多实例资源类型）
- Available[m]：各资源类型可用实例数
- Allocation[n×m]：分配给各进程的资源
- Request[n×m]：各进程当前请求的资源

算法（类似银行家算法的安全检查）：
1. Work = Available；未完成进程 Finish = false
2. 找 Finish[i]=false 且 Requestᵢ ≤ Work 的进程
3. Work += Allocationᵢ；Finish[i]=true；重复
4. Finish[i]=false 的进程即为死锁进程

---

## 五、死锁预防（破坏四个条件）

### 7. 破坏条件的方法

| 破坏条件 | 方法 | 问题 |
|----------|------|------|
| **Mutual Exclusion** | 使资源可共享 | 对不可共享资源不可行 |
| **Hold and Wait** | 进程启动前申请所有资源 | 资源利用率低，饥饿可能 |
| **No Preemption** | 若请求被拒则释放已持有资源 | 语义复杂 |
| **Circular Wait** | 资源类型全序编号，按递增序请求 | 合理可行 |

---

## 六、死锁避免（Deadlock Avoidance）

### 8. Safe State（安全状态）
- **Safe State**：存在安全序列 <P₁,...,Pₙ> 使得每个 Pᵢ 的最大需求可被当前可用 + 所有 Pⱼ(j<i) 持有的资源满足
- **Safe → 无死锁**
- **Unsafe → 可能死锁**
- 死锁避免 = 确保系统永不进入 Unsafe State

### 9. Banker's Algorithm（银行家算法）

**数据结构**：
- `Available[m]`：可用资源
- `Max[n×m]`：每个进程的最大需求
- `Allocation[n×m]`：已分配
- `Need = Max - Allocation`

**Safety Algorithm**：
1. Work = Available, Finish = false
2. 找 Finish[i]=false 且 Needᵢ ≤ Work
3. Work += Allocationᵢ, Finish[i]=true, 重复
4. 全部 Finish=true → Safe

**Request Algorithm**：
1. Request ≤ Need（否则错误：超出最大声明）
2. Request ≤ Available（否则等待）
3. 假装分配：Available-=Request, Allocation+=Request, Need-=Request
4. 运行 Safety Algorithm → Safe? 正式分配 : 恢复原状并等待

**例题（课件中的安全检查）**：
5 个进程 P0-P4，3 种资源 A(10), B(5), C(7)
- 安全序列：<P1, P3, P4, P2, P0>
- P1 request (1,0,2) → Safe ✓
- P4 request (3,3,0) → 检查
- P0 request (0,2,0) → 检查

---

# L11 — I/O and Storage（I/O 与存储）

---

## 一、设备接口

### 1. 设备的三类寄存器
| 寄存器 | 功能 |
|--------|------|
| **Status Register** | 检查设备当前状态 |
| **Command Register** | 通知设备执行特定任务 |
| **Data Register** | 向设备传递/获取数据 |

- 现代设备（如 NVMe SSD）使用多命令队列（最深 64K）替代单一命令寄存器

---

## 二、I/O 方式

### 2. Polling（轮询）
- 写入流程：等设备就绪 → 写数据 → 写命令 → 轮询等待完成
- **问题**：浪费 CPU 时间等待慢速设备；如果 CPU 切换到其他任务，数据可能被覆盖

### 3. Interrupts（中断）
- 替代轮询：OS 发出请求 → 将进程休眠 → 上下文切换
- 设备完成后**触发硬件中断** → CPU 跳转到中断处理程序

**硬件支持**：
- 中断请求线（IRQ）
- 中断控制器（x86: APIC, ARM: GIC, RISC-V: PLIC/CLINT）
- 每条指令后检查中断；保存 CPU 状态；跳转处理程序

**软件支持**：
- 中断向量表：将中断分派到正确的处理程序
- 中断链（Interrupt Chaining）：多个设备共享同一中断号
- 中断优先级：高优先级中断可抢占低优先级中断

### 4. Programmed I/O (PIO) vs. Memory-Mapped I/O (MMIO)
| | PIO | MMIO |
|---|-----|------|
| 方式 | 专用 I/O 指令（x86: in/out） | load/store 指令 |
| 地址 | 独立 I/O 空间 | 寄存器映射到物理地址空间 |
| 保护 | I/O 指令是特权指令 | 通过地址转换保护 |
| 优势 | — | 现代主流，性能更好 |

---

## 三、DMA（Direct Memory Access）

### 5. DMA 机制
- **目的**：避免 PIO 在大数据传输时消耗 CPU
- OS 写入 DMA 命令块（源/目标地址、读写模式、字节数）→ DMA 控制器
- DMA 控制器通过 **Bus Mastering** 从 CPU 夺取总线
- **Cycle Stealing**：从 CPU 窃取总线周期 → 仍比 PIO 高效
- 完成后发送中断通知

### 6. DMA 传输流程
CPU 告知设备驱动传输 → 设备驱动告知磁盘控制器 → 磁盘控制器初始化 DMA → DMA 控制器逐字节传输到内存 → 完成后 DMA 中断 CPU

---

## 四、存储设备

### 7. 磁盘性能计算（重要！）
**假设**：
- 平均寻道时间 5ms
- 7200RPM → 旋转一圈 = 60000/7200 ≈ 8ms → 平均旋转延迟 4ms
- 传输速率 4MB/s，扇区大小 1KB → 传输 1KB = 0.26ms

**随机位置读**：5 + 4 + 0.26 ≈ **10ms** → 约 100KB/s
**同柱面随机读**：4 + 0.26 ≈ **5ms** → 约 200KB/s
**同一磁道下一扇区**：0.26ms → 约 **4MB/s**

**关键**：最小化寻道和旋转延迟是高效使用磁盘的关键！

### 8. HDD 典型参数
- 容量：8-10TB；面密度 > 1Terabit/sq inch
- 平均寻道：5-10ms
- 转速：3600-7200 RPM（笔记本），15000 RPM（服务器）
- 传输率：50-100 MB/s

### 9. SSD 关键挑战
- **写耐久度**：有限的编程/擦除周期
- **垃圾回收 & 磨损均衡**：固件均匀分布写入
- **性能一致性**：满盘或持续写入后变慢

---

# L12 — File System（文件系统）

---

## 一、文件系统基本概念

### 1. 文件系统定义
- 将磁盘的块接口转换为**文件和目录**
- 四大组件：**命名**（按名找文件）、**磁盘管理**（块收集为文件）、**保护**、**可靠性/持久性**

### 2. 三种视角
| 视角 | 文件是什么 |
|------|-----------|
| 用户视角 | 持久数据结构 |
| 系统调用接口 | 字节集合（UNIX） |
| OS 内部 | 块集合（UNIX: 4KB block） |

### 3. 用户-系统视图转换
- 读 bytes 2-12：取对应块 → 返回块中正确部分
- 写 bytes 2-12：取块 → 修改部分 → 写回块
- 所有内部操作以**整块**为单位

---

## 二、文件分配方式

### 4. Contiguous Allocation（连续分配）
- 文件占据磁盘上连续区域
- **优点**：定位快（只需起始地址）
- **缺点**：
  - **外部碎片**（有空间但没有足够大的连续洞）
  - **增长问题**（需要预分配或移动）
- 应用：ISO 9660 (CD-ROM)

### 5. Linked Allocation（链式分配）
- 文件由链表连接的块组成
- 每块末尾留 4 字节指向下一块
- 根目录存储文件名 + 第一块号 + 大小
- **优点**：无外部碎片，文件可增长
- **缺点**：
  - **内部碎片**（最后一块不完整）
  - **随机访问差**（访问第 N 块需遍历 N-1 个指针）

### 6. FAT（File Allocation Table）
- 将所有块链接**集中**到一个表中
- 每个 FAT 条目 = 下一块号（EOF = -1）
- 目录条目存 `First block #`
- 读取流程：目录→FAT→跟随链→直到 EOF
- **FAT 变体**：
  | 类型 | 地址位数 | 最大簇数 |
  |------|----------|----------|
  | FAT12 | 12 bits | 4096 |
  | FAT16 | 16 bits | 65536 |
  | FAT32 | 28 bits | 2²⁸ |

- **FAT 布局**：Boot Sector → FSINFO → FAT(×2备份) → Root Directory → Data

### 7. FAT 操作
- **读文件**：目录 → 起始簇 → FAT 链 → 以文件大小确定最后簇
- **追加**：找最后簇 → 通过 FSINFO 分配新空闲簇 → 更新 FAT+FSINFO+文件大小
- **删除**：释放 FAT 链（全置 0）→ 目录条目第一字节改为 0xE5
- **恢复删除文件**：立即断电！第一簇地址在目录条目中仍可读；文件大小决定需要几个簇
- **安全删除**：覆盖数据（普通删除只标空闲，不擦数据）

### 8. FAT 目录条目（32 字节）
| 字节 | 内容 |
|------|------|
| 0 | 文件名首字符（0x00 或 0xE5=未分配） |
| 1-10 | 文件名+扩展名（8+3 命名约定） |
| 11 | 文件属性（只读、隐藏等） |
| 20-21 | 首簇地址高 2 字节 |
| 26-27 | 首簇地址低 2 字节 |
| 28-31 | 文件大小（4GB-1 上限） |

### 9. LFN（Long File Name）
- 用额外的目录条目存储长文件名（unicode，每条目 13 字符）
- LFN 条目的属性字节 = 0x0F
- 正常条目仍然在 LFN 条目之后

---

## 三、Unix iNode 文件系统

### 10. iNode 结构
- 所有文件指针集中在一个 inode 中（vs FAT 中指针分散）
- 一个文件/目录有一个 inode
- **不平衡树结构**，固定大小块

### 11. iNode 数据块指针
| 类型 | 数量 | 可达大小（4KB block） |
|------|------|----------------------|
| Direct Pointers | 12 | 48 KB |
| Single Indirect | 1 | 48KB + 4MB |
| Double Indirect | 1 | + 4GB |
| Triple Indirect | 1 | + 4TB |

- 间接指针指向一个装满指针的磁盘块
- 4KB block ÷ 4 字节地址 = 1024 个指针/块

### 12. iNode 元数据
- 文件类型和权限、User ID、Group ID
- 文件大小、时间信息
- Link count（硬链接数）

### 13. Ext2/3 磁盘布局
- 划分为多个 **Block Group**（性能+可靠性）
- 每个 Block Group：
  ```
  Superblock | GDT | Reserved GDT | Block Bitmap | iNode Bitmap | iNode Table | Data Blocks
  ```
- Superblock 和 GDT 在**每个 block group 中复制**（可靠性！）
- 位图管理：Block Bitmap（0=空闲, 1=已分配），iNode Bitmap（同理）

### 14. Ext2/3 目录条目
| 字节 | 内容 |
|------|------|
| 0-3 | iNode 号 |
| 4-5 | 本条目的长度 |
| 6 | 文件名长度 |
| 7 | 文件类型 |
| 8+ | 文件名（最长为 255 字符的 ASCII） |

### 15. Hard Link（硬链接）
- 创建指向已有 inode 的新目录条目
- inode 中的 **link count** 递增
- 多个路径名指向同一文件
- 删除文件 = `unlink()` → link count - 1 → 归 0 时释放数据块和 inode

### 16. Symbolic Link（软链接 / 符号链接）
- 创建**新 inode**（含目标路径名）
- 若路径名 ≤ 60 字符 → 存在 inode 的指针空间内
- 若路径名 > 60 字符 → 使用数据块存储
- 删除目标文件会导致软链接断裂

---

## 四、Memory Mapped Files（mmap）

### 17. mmap() 机制
- 将文件**直接映射**到进程地址空间
- 访问映射区域时隐式 page-in（通过 page fault 加载文件内容）
- 写入 → 最终 page-out 到文件
- 避免传统 I/O 的多次拷贝和系统调用

---

# L13 — Operating System Security（操作系统安全）

---

## 一、Confinement（限制）

### 1. 为什么需要限制
- 系统必须运行不受信任的代码（移动应用、JavaScript、浏览器扩展、第三方设备驱动）
- 限制要求：阻止访问其他应用的内存/文件、阻止执行内核级操作、阻止故障干扰系统

### 2. 实现限制的三大方法

| 方法 | 说明 |
|------|------|
| **Isolation** | 边界强制执行：各组件独立运行，防止干扰或故障传播 |
| **Access Control** | 权限管理：谁可以对什么资源做什么操作 |
| **TEE** | 硬件/软件扩展，保护敏感应用不受不可信 OS/VMM 侵犯 |

---

## 二、Isolation（隔离）

### 3. 隔离层次

| 层次 | 实现 |
|------|------|
| **物理隔离** | 在物理分离的计算机上运行 |
| **硬件隔离** | 用户态 vs 内核态、进程、Intel MPK、Arm MTE |
| **软件隔离** | eBPF、SFI、WebAssembly |

### 4. Software Fault Isolation (SFI)
- 运行时或编译时插桩实现安全边界
- **目标**：安全性（隔离不可信模块）、可移植性（无需硬件支持）
- **代价**：5%-20% 性能开销
- 示例：Google Native Client (NaCl)
  - 所有间接跳转必须对齐 32 字节边界
  - 数据访问掩码在 4GB 沙箱内

### 5. Intel MPK（Memory Protection Keys）
- 4-bit protection key 存于 PTE
- PKRU 寄存器（32-bit）控制每 key 的访问权限
- 每 key 有 2 bit：(AD, WD) — Access Disable, Write Disable
- 16 个不同的页组可同时更改权限

### 6. eBPF（Extended Berkeley Packet Filter）
- Linux 内核中的安全高效虚拟机
- 运行用户定义程序（无需修改内核源代码或加载模块）
- 用途：网络（XDP）、安全（检测恶意系统调用）、追踪
- 工作流：加载字节码 → 验证（无循环、合法内存访问）→ JIT 编译执行

---

## 三、Access Control（访问控制）

### 7. 基本概念
- **Subject**：用户/进程
- **Object**：文件/设备
- **Permission**：读/写/执行

### 8. Access Control Matrix
- 二维矩阵：(subject, object) → 权限集合

### 9. ACL vs. Capability

| 特性 | ACL | Capability |
|------|-----|------------|
| 存储位置 | 附属于对象（如文件） | 由主体持有（如 token） |
| 扩展性 | 较慢（检查中央 ACL） | 较快（本地检查） |
| 委托 | 较难（需修改 ACL） | 较易（分享 capability） |
| 示例 | Linux 文件权限 | OAuth tokens, AWS IAM keys |

### 10. Capability 的关键属性
- **不可伪造**（加密签名或硬件强制）
- **可委托**（可动态分享或撤销）
- **自描述**（包含对象引用和权限）
- **无需全局检查**

### 11. Linux DAC vs. MAC
| | DAC | MAC |
|---|-----|-----|
| 控制基础 | 用户/进程身份 + 权限 | 安全标签/分类 |
| 谁设权限 | 资源所有者 | 中央权威 |
| 实现 | chmod, chown | SELinux, AppArmor |

---

## 四、TEE（Trusted Execution Environment）

### 12. 主流 TEE 实现
- Intel SGX / TDX
- Arm TrustZone / CCA
- AMD SEV

### 13. TEE 的关键技术
- **CPU 隔离**：外部软件无法访问 TEE 内的机密数据（内存、寄存器、CPU 缓存）
- **内存加密**：物理内存被硬件加密引擎实时加密
- **远程认证**：用户可验证 TEE 运行软件的初始状态完整性

---

## 五、System Hardening（系统加固）

### 14. 漏洞发现技术

| 类别 | 方法 | 工具 |
|------|------|------|
| **Static Analysis** | 不执行代码分析 | Cppcheck, FlowDroid, Angr, SAGE |
| **Dynamic Analysis** | 测试运行中应用 | AFL++, libFuzzer, TaintDroid |
| **Hybrid** | 符号+具体执行 | KLEE, QSYM |

### 15. Fuzzing（模糊测试）
- 注入随机/畸形/意外输入来发现 bug
- 分类维度：
  - 输入生成：Mutation-Based vs. Generation-Based
  - 反馈机制：Coverage-Guided vs. Black-Box vs. Grey-Box
  - 目标知识：White-Box vs. Black-Box vs. Hybrid

### 16. ASLR（Address Space Layout Randomization）
- 随机化栈、堆、库的虚拟地址
- 缓解 ROP、jump-to-libc 攻击
- 强制攻击者猜测内存地址
- 使 exploit 更难但不完全不可能

### 17. Stack Canary（栈金丝雀）
- 编译器插入的随机值（在栈变量和返回地址之间）
- 缓冲区溢出会覆盖 canary → 检测到修改 → 程序终止
- 防止栈缓冲区溢出利用
- **局限**：不能防止堆溢出或纯数据攻击；若攻击者能泄露或猜测 canary 则可绕过

### 18. CFI（Control Flow Integrity）
- 限制程序执行到合法的控制流路径
- 防止 ROP、JOP、调用/返回劫持
- **Forward-Edge CFI**：保护间接调用/跳转（Clang CFI, Intel CET IBT）
- **Backward-Edge CFI**：保护函数返回（Shadow Stack）
- **硬件支持**：Intel CET（Shadow Stack + IBT）、ARM BTI
- 约 90% ROP 攻击被阻止，1-15% 运行时性能代价

---

## 六、Detection（检测）

### 19. 恶意软件类型
| 类型 | 特点 |
|------|------|
| Virus | 自我复制，感染文件 |
| Worm | 自我传播，通过网络传播 |
| Trojan | 伪装为合法软件 |
| Ransomware | 加密数据，要求赎金 |
| Spyware | 秘密收集信息 |
| Rootkit | 获得 root 权限，隐藏存在 |

### 20. 恶意软件检测技术
- Signature-Based（签名）→ Heuristic-Based（启发式）→ Behavioral Analysis（行为）→ ML-Based（机器学习）→ Sandboxing（沙箱）→ Anomaly Detection（异常检测）

### 21. HIDS（Host-based Intrusion Detection）
- 组件：Event Sensors → Event Analyzers → Response Mechanisms
- 检测技术：Signature、Anomaly、Behavioral、Log Analysis

---

> 📖 **完整内容请对照课件 PDF 原文。** L08 的详细知识点请参见单独的 L08 总结文档。
